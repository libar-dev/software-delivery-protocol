# Plan 37 — Adoption tranches, drift maturation, and bundle measurement

> **Status:** 🔄 EXECUTING — the plan-36 arc is executing; briefs I–K delivered per plans/36; operational tracking in `.omo/plans/plan-37-settling-arc.md`

**In.** Briefs **I** through **K** from `plans/36-adoption-tranches-maturation-and-bundle-evidence-briefs.md`:

- **I** later registrar adoption tranches under MD-31. Each remaining authored self-hosting family is adopted when the frozen five-adapter surface expresses it without contortion, or refused with an honest reason. The plan-35 F-ledger shape is the record: outcome, evidence, and generated sibling.
- **J** honest dispositions for the eight recurring `implemented ∧ ¬ready` Specs. Promotion is a human statement: owner-ratified one-rung edits are lawful; a Spec that remains `defined` carries its blocking reason, and no Spec is demoted to silence the alarm.
- **K** measurement of the plan-35 context-bundle deferral. Define and gather evidence from this arc's sessions, then commission a later plan only if repeated hand-assembly is demonstrated. The bundle is never built in this arc.

**Out.** The plan-36 do-not-reopen list remains binding: no default-carrier flip; no Gherkin kind expansion, DocStrings/DataTables, or Gherkin Packs; no `implements` slot; no registrar-freeze loosening; no O5 engine-side adopter execution; no re-specifying shipped projections; no `bySymbol` or impact graph; no `.sdp.gherkin` suffix change; no new query verb or reader accessor; no E2 placement reversal; and no reopening the E3 MCP non-ruling or the three event-triggered H deferrals. Do not edit `docs/agent-surface/recipes.md`, the frozen registrar interface, shared helpers, corpus count pins, or any carrier without the stated owner-ratification path. No content is invented to reach a readiness rung, and no bundle implementation is included.

Re-measured at commission, never inherited: **156 Specs · 1 Pack · 157 anchors → 314 nodes · 660 edges; 0 errors, 0 warnings.** Recipe 1 is empty: `total: 0`, with `excludedReadyExamples: 66`, `excludedReadyDecisions: 31`, and `excludedWithoutVerifier: []`. Recipe 2 has **8** alarms, naming the same eight Specs — `spec:carrier.markdown-authoring`, `spec:consumers.projections-model`, `spec:extraction.claim-taxonomy`, `spec:extraction.regenerability`, `spec:model.core-model`, `spec:model.pack-aggregate`, `spec:model.relations`, and `spec:model.spec-sections`; all are stated `defined`, have `floorReached: ready`, and have no unmet floor clause. Recipe 8 is clean: `errors: 0`, `warnings: 0`, `byValidator: {}`, `signals: []`. The deferred-adoption census is **58 `bindExample` call sites** across eight authored suites: carrier-gherkin 13, validators 16, projections 11, extraction 9, consumers 5, pack-markdown 2, carrier 1, and sdp-import 1.

## Execution record

This is the execution stamp for the plan-36 arc. The plan-36 briefs index remains DRAFTED lineage; operational tracking lives in `.omo/plans/plan-37-settling-arc.md`.

### I later-tranche ledger (MD-31)

Every one of the 58 deferred `bindExample` sites is accounted for. 46 adopted onto tracked generated siblings. 12 refused and remain on `bindExample`. The frozen five-adapter interface was not edited. Refusal is a complete family or example outcome.

#### I-0 tracer

| Family | Sites | Outcome | One-line evidence |
| --- | --- | --- | --- |
| `carrier.markdown-parser` | `bounded-parity` | **ADOPT** | Split TypeScript vs Markdown severity/count Thens live in `assertions` via `paramsForStep`. `.omo/evidence/task-2-plan-37-settling-arc.md`. |

Tracked sibling: `test/carrier.markdown-parser.bounded-parity.test.generated.ts`.

#### I-1 validators, 16 sites / 9 families

| Family | Sites | Outcome | One-line evidence |
| --- | --- | --- | --- |
| `validation.readiness-floor` | `unrelated-scoped-spec`, `blocking-open-question` | **ADOPT** | One `validateGraph`. Clause id, error count, and subject joins live in `assertions`. |
| `validation.warn-level-signals` | `orphan-signal`, `ready-gap-signal` | **ADOPT** | Observe maps the sole warning class. Error-count 0 and subject join ride `assertions`. |
| `validation.referential-integrity` | `dangling-target`, `did-you-mean` | **ADOPT** | Named `conformance/referential-integrity` finding. Did-you-mean text lives in `assertions`. |
| `validation.authored-honesty` | `section-authored-fact`, `unearned-stated-fact` | **ADOPT** | Named honesty finding. relatedId, phrase, and subject joins live in `assertions`. |
| `validation.claim-separation` | `collapsed-edge-claim`, `unratified-descriptor` | **ADOPT** | Named `conformance/claim-separation`. Phrase and floor-count Thens live in `assertions`. |
| `validation.verification-linkage` | `unbound-example`, `unresolved-oracle` | **ADOPT** | When is one `createReader`. Observe returns actual `finding.severity`. `has-verifier` join is a pure read. |
| `validation.oracle-target-eligibility` | `rule-space-accepted`, `missing-space-refused` | **ADOPT** | Same one-call SpecContext mapping. Finding-count and oracle-present are product values. |
| `validation.pack-coherence` | `incoherent-aggregate` | **ADOPT** | Named `conformance/pack-coherence`. Finding-count 2 rides `assertions`. |
| `validation.two-check-families` | `split-report` | **ADOPT** | Comparator owns the no-params kind string. Both named-family Thens are assertion-owned. |

Evidence: `.omo/evidence/task-5-plan-37-settling-arc.md`. Tracked siblings: `test/validation.readiness-floor.{unrelated-scoped-spec,blocking-open-question}.test.generated.ts`, `test/validation.warn-level-signals.{orphan-signal,ready-gap-signal}.test.generated.ts`, `test/validation.referential-integrity.{dangling-target,did-you-mean}.test.generated.ts`, `test/validation.authored-honesty.{section-authored-fact,unearned-stated-fact}.test.generated.ts`, `test/validation.claim-separation.{collapsed-edge-claim,unratified-descriptor}.test.generated.ts`, `test/validation.verification-linkage.{unbound-example,unresolved-oracle}.test.generated.ts`, `test/validation.oracle-target-eligibility.{rule-space-accepted,missing-space-refused}.test.generated.ts`, `test/validation.pack-coherence.incoherent-aggregate.test.generated.ts`, `test/validation.two-check-families.split-report.test.generated.ts`.

#### I-2 Gherkin, 13 sites / 1 family

| Family | Sites | Outcome | One-line evidence |
| --- | --- | --- | --- |
| `carrier.gherkin-authoring` | 13 | **SPLIT: ADOPT 11 / REFUSE 2** | Eleven examples need one `extract` plus read-only report/graph joins. `unbound-ready-refused` needs `extract` then `validateGraph`. `contract-parity` rematerializes corpora and adds extract, `serializeGraph`, and `generateContracts` calls. |

Evidence: `.omo/evidence/task-6-plan-37-settling-arc.md`. Tracked siblings: `test/carrier.gherkin-authoring.{authored-fact-refused,description-location-refused,duplicate-surface-refused,example-space-extraction,malformed-relation-refused,missing-id-refused,multi-finding-bounded,parent-child-extraction,step-less-scenario-refused,unknown-tag-refused,unsupported-construct-refused}.test.generated.ts`. Untracked owed siblings: `test/carrier.gherkin-authoring.{contract-parity,unbound-ready-refused}.test.generated.ts`.

#### I-3 projections, 11 sites / 5 families

| Family | Sites | Outcome | One-line evidence |
| --- | --- | --- | --- |
| `consumers.design-review` | `pure-projection` | **ADOPT** | When is the authored two-run identity law. Index-page set is comparator-owned. |
| `consumers.derived-readiness-banner` | `dishonest-divergence`, `honest-headroom` | **ADOPT** | One `renderDesignReview`. Observe returns the product floor string. |
| `consumers.binding-language-views` | `bound-spec-page`, `pack-member-table` | **SPLIT: REFUSE / ADOPT** | `pack-member-table` point is complete. `bound-spec-page` omits `packId`; registrar completeness refuses oracle comparison. |
| `consumers.wholesale-view-rewrite` | `stale-page-removed`, `late-stale-page`, `failed-run-view-removed`, `build-invalidates-view` | **ADOPT** | One `runView` or `runBuild`. Late plant is an extract hook inside that call. |
| `validation.diagnostic-rendering` | `composed-location`, `table-cell-location` | **ADOPT** | One renderer applied to the three location shapes. Observe returns product text. |

Evidence: `.omo/evidence/task-12-plan-37-settling-arc.md`. Tracked siblings: `test/consumers.design-review.pure-projection.test.generated.ts`, `test/consumers.derived-readiness-banner.{dishonest-divergence,honest-headroom}.test.generated.ts`, `test/consumers.binding-language-views.pack-member-table.test.generated.ts`, `test/consumers.wholesale-view-rewrite.{stale-page-removed,late-stale-page,failed-run-view-removed,build-invalidates-view}.test.generated.ts`, `test/validation.diagnostic-rendering.{composed-location,table-cell-location}.test.generated.ts`. Untracked owed sibling: `test/consumers.binding-language-views.bound-spec-page.test.generated.ts`.

#### I-4 extraction, 9 sites / 5 families

| Family | Sites | Outcome | One-line evidence |
| --- | --- | --- | --- |
| `extraction.build-pipeline` | `same-invocation` | **ADOPT** | One `runSdpCli q --json`. Exit code is comparator-owned. |
| `extraction.excludes` | `segment-boundary`, `refused-path` | **ADOPT** | One `discoverFiles`. Discovery outcome is comparator-owned. |
| `extraction.schema-versioning` | `declared-version` | **ADOPT** | Given materializes `deriveGraph`. When is one `serializeGraph`. |
| `extraction.executable-contracts` | `concreteness-refusal`, `multi-entry-example`, `case-colliding-path` | **REFUSE** | Siblings hardcode `dimension, exampleId, binding, entryCount, twinId`. Each authored point is partial. Completeness check fires before adapters. |
| `extraction.example-runner` | `step-order`, `red-step-naming` | **SPLIT: REFUSE / ADOPT** | `red-step-naming` binds every required condition. `step-order` omits `failingPhase` and `thrown`. |

Evidence: `.omo/evidence/task-13-plan-37-settling-arc.md`. Tracked siblings: `test/extraction.build-pipeline.same-invocation.test.generated.ts`, `test/extraction.excludes.{segment-boundary,refused-path}.test.generated.ts`, `test/extraction.schema-versioning.declared-version.test.generated.ts`, `test/extraction.example-runner.red-step-naming.test.generated.ts`. Untracked owed siblings: `test/extraction.executable-contracts.{concreteness-refusal,multi-entry-example,case-colliding-path}.test.generated.ts`, `test/extraction.example-runner.step-order.test.generated.ts`.

#### I-5 tails

| Family | Sites | Outcome | One-line evidence |
| --- | --- | --- | --- |
| `carrier.markdown-pack-authoring` | `markdown-ts-parity`, `spec-envelope-refused` | **ADOPT** | Filesystem `createWorld` like `model.anchors`. Refusal example records `extract/unrecognized-property`. `.omo/evidence/task-7-plan-37-settling-arc.md`. |
| `consumers.agent-surface` | `scripted-context-body`, `demand-map-entries` | **REFUSE** | Siblings require all five parent-space conditions. Authored points omit `concept/file/unrecordedFile` and `specId`. `.omo/evidence/task-8-plan-37-settling-arc.md`. |
| `consumers.reader` | `concept-entry`, `file-entry`, `changeset-entry` | **REFUSE** | Siblings require all six parent-space conditions. Authored points omit the unrelated dimensions. Same completeness refuse. |
| `carrier.sdp-import` | `round-trip` | **ADOPT** | Fixture source in `createWorld`. When is one `importTypeScriptSpec`. `.omo/evidence/task-9-plan-37-settling-arc.md`. |

Tracked siblings: `test/carrier.markdown-pack-authoring.{markdown-ts-parity,spec-envelope-refused}.test.generated.ts`, `test/carrier.sdp-import.round-trip.test.generated.ts`. Untracked owed siblings: `test/consumers.agent-surface.{scripted-context-body,demand-map-entries}.test.generated.ts`, `test/consumers.reader.{concept-entry,file-entry,changeset-entry}.test.generated.ts`.

#### Frozen-adapter friction, accumulated and not repaired

1. The comparator matches one Outcome kind. Extra Thens stay in `assertions` via `paramsForStep`. Same pressure as plan 35 kind-evidence.
2. Generated `invoke` is one product call. That is honest for extract-only Gherkin examples and a refuse for `unbound-ready-refused` and `contract-parity`, whose Thens need later engine calls.
3. Registrar `requiredConditions` are the parent space, not the authored point. Partial points refuse oracle comparison before `createWorld` runs. Hits: `consumers.agent-surface` 2, `consumers.reader` 3, `consumers.binding-language-views.bound-spec-page`, `extraction.executable-contracts` 3, `extraction.example-runner.step-order`.
4. `createReader` as the one When is lawful. Pairing it with a prior `validateGraph` would be two calls. Verification-linkage and oracle-target use the one-call mapping.
5. Cleanup registries are not worlds. `temporaryRoots` only lets `afterEach` remove freshly minted roots.
6. The freeze was not loosened. Friction stays here for a later ruling.

#### 68 owed siblings, reconciled against the live tree

`generated/registrars.json` lists 68 files. First-tranche ten remain tracked: `test/model.stable-ids.{namespaced-round-trip,malformed-refusal}.test.generated.ts`, `test/model.anchors.{physical-identity,lookalike-refusal}.test.generated.ts`, `test/carrier.slot-notation.{typed-declaration,refused-guess}.test.generated.ts`, `test/validation.duplicate-ids.dual-carrier.test.generated.ts`, `test/validation.kind-evidence.{constraints-alone,empty-promoted-child,untargeted-constraint}.test.generated.ts`.

Later-tranche tracked siblings: 46. Untracked owed refusals: 12. Live `bindExample(` census: 12, matching those 12 refusals.

`10 + 46 + 12 = 68`.

### J drift-alarm disposition

Owner ratification `2026-08-20`, bundle `.omo/evidence/plan-37-j-packets/RATIFICATION-BUNDLE.md`. Applied by Todo 16. Five READY carriers moved one rung. Three DEFINED carriers were left untouched. No Spec was demoted.

| Spec | Disposition | Reason |
| --- | --- | --- |
| `spec:carrier.markdown-authoring` | **READY** | Owner ratified. Complete invariant with carrier surface refined by children; no blocker found. |
| `spec:consumers.projections-model` | **DEFINED** | Impact-graph and measurement work remain unlanded; plan 35 explicitly retained `defined`. |
| `spec:extraction.claim-taxonomy` | **READY** | Owner ratified. Complete bounded claims glossary; no blocker found. |
| `spec:extraction.regenerability` | **DEFINED** | Quoted thresholds lack their measurement artifact. |
| `spec:model.core-model` | **DEFINED** | Blocking enrichment-lifecycle question remains open. |
| `spec:model.pack-aggregate` | **READY** | Owner ratified. No blocking Pack contract found. |
| `spec:model.relations` | **READY** | Owner ratified. Complete typed relation vocabulary; no blocker found. |
| `spec:model.spec-sections` | **READY** | Owner ratified. Strongest packet; no blocker found. |

Fresh recipe 2 membership, re-derived in this close task, is exactly the three DEFINED rows. Each is stated `defined`, `floorReached: ready`, `firstUnmetClause: null`. Implementation bindings: projections-model 2, regenerability 1, core-model 3. The five READY ids are absent from the alarm.

The five READY Specs now emit `honesty/gaps` warnings. Recipe 8 reports them. That is informative. Ready never requires a verifier. Todo 20 owns whether the full gate is green.

### K context-bundle verdict

**STAND-DOWN (unmet)**

Eligible sessions: 12 = 8 I + 4 J. Qualifying episodes: 0. Shared normalized recipe core: empty. The plan-35 re-entry trigger is retained. No later plan was commissioned. No bundle was built.

Arithmetic lives in `.omo/evidence/plan-37-k-measurement/verdict.md`. Corpus is adequate, `12 >= 6`, I `8 >= 2`, J `4 >= 2`. Every trigger conjunct after adequacy fails. Todo 16 is QA-only and excluded from eligibility. Todo 14's apparent window is quoted `rg` text, not executed recipes.


## Commission evidence

The measurements above were obtained by re-running the plan-36 "How to consume" commands against the live tree at commission. Exact commands and outputs, including the pre-edit discovery-gate run, are recorded in `.omo/evidence/task-1-plan-37-settling-arc.md`.

## Close record, 2026-08-20

Numbers below were measured in Todo 18 against the live tree. They were not copied from the commission stamp, from plan 35, or from any prior evidence file. Each command ran twice in this worker session. Outputs were identical. This record does not claim the Todo-20 gate is green.

## Re-derived at close

Canonical validate, this close task, exit 0 both passes:

`npm run --silent sdp -- validate . --exclude explorations --exclude examples --exclude test/fixtures/import/parity`

`156 specs · 1 packs · 157 anchors → 314 nodes · 660 edges (0 errors, 0 warnings)`

That first line is derivation. The same run then printed five informative honesty warnings and closed with `validate: 0 errors · 5 warnings (conformance + honesty over the one graph)`.

Warning subjects, both passes, all `honesty/gaps`:

- `spec:carrier.markdown-authoring`
- `spec:extraction.claim-taxonomy`
- `spec:model.pack-aggregate`
- `spec:model.relations`
- `spec:model.spec-sections`

Recipe bodies 1, 2, and 8 were lifted from the live `docs/agent-surface/recipes.md` fences and run through `pnpm --silent sdp:q '<body>' --json`.

- Recipe 1, build backlog: `total: 0`, `byFamily: {}`, `excludedReadyExamples: 66`, `excludedReadyDecisions: 31`, `excludedWithoutVerifier: []`.
- Recipe 2, drift alarm: `total: 3`. The three Specs, all stated `defined`, with `floorReached: ready` and `firstUnmetClause: null`: `spec:consumers.projections-model` (2 implementation bindings), `spec:extraction.regenerability` (1), `spec:model.core-model` (3).
- Recipe 8, orphans and gaps: `errors: 0`, `warnings: 5`, `byValidator: { "honesty/gaps": 5 }`. Signals name the same five READY subjects listed above.

Bind census, both passes:

`rg -o 'bindExample\(' test/self-hosting-*.test.ts --count-matches`

`test/self-hosting-projections.test.ts:1`
`test/self-hosting-extraction.test.ts:4`
`test/self-hosting-carrier-gherkin.test.ts:2`
`test/self-hosting-consumers.test.ts:5`

Sum 12. `git ls-files -- 'test/*.test.generated.ts'` is 56. `generated/registrars.json` `files.length` is 68. `10` first-tranche + `46` later adopted + `12` refusals = `68`.

Raw tails, both-pass comparison, and hashes live in `.omo/evidence/task-18-plan-37-settling-arc.md`.

### Drift-alarm and graph delta versus the independently sourced commission baseline

Commission, Todo 1 live run, recorded on this stamp and in `.omo/evidence/task-1-plan-37-settling-arc.md`, and this close task were sourced separately. Comparison happens only after both exist.

| Measurement | Commission | This close | Delta |
| --- | --- | --- | --- |
| Specs / Packs | 156 / 1 | 156 / 1 | unchanged |
| Anchors → nodes · edges | 157 → 314 · 660 | 157 → 314 · 660 | unchanged |
| Validate findings | 0 errors, 0 warnings | 0 errors, 5 `honesty/gaps` warnings | +5 warnings on the five READY Specs, which have no resolving verifier |
| Recipe 1 backlog | empty, `total: 0`, exclusions 66 / 31 | empty, `total: 0`, exclusions 66 / 31 | unchanged |
| Recipe 2 drift-alarm set | 8 Specs, all `defined` | 3 Specs, the DEFINED rows above | -5, the owner-ratified READY set left the alarm |
| Recipe 8 warnings | 0 | 5 `honesty/gaps` | +5, same five subjects |
| `bindExample` census | 58 | 12 | -46 adopted; 12 refusals remain |
| Tracked generated siblings | 10 | 56 | +46 later-tranche adoptions |
| Owed `generated/registrars.json` files | 68 | 68 | unchanged |

Cause, grounded in this arc's landed changes: Brief I is test-side registrar adoption. Graph identity did not move. Brief J stated `ready` on five already-`implemented` Specs. They leave recipe 2 and appear as `honesty/gaps` because they have no verifier binding. Brief K measured sessions and built nothing. Todo 20 still owns the consecutive full-gate runs and the EXECUTED status flip.

## Independent review register, Todo 19

Review boundary: commission baseline `a8d5898f549778d5841653dc81730a0c5810e446`, commission commit `1dae853a6f51bdcb8c4bde16d84dafda611e0fca`, reviewed head `8e6a86bf946dd28958a01d5034ce849d97d88731`. The reviewer inspected every ancestry-path commit and re-ran the current-tree measurements. Full evidence: `.omo/evidence/task-19-plan-37-settling-arc.md`.

| ID | Severity | Finding | Disposition | Owner | Closure evidence | Status |
| --- | --- | --- | --- | --- | --- | --- |
| P37-R1 | MEDIUM | Commit `4851740` changed `contract-dependent-suites.mjs` and `eslint.config.js` without the original Plan 37 no-check-change boundary authorizing it. | **ACCEPT** | Plan 37 owner; independently closed in Todo 19 | Owner statement `Approve Brief-I exception` (2026-08-20) and the operational-plan amendment authorize only the exact 56 tracked generated siblings and the one rule disable inside the existing contract-dependent-suite override. Current inventory equals the tracked set with no leak. No other check/config implementation changed. Lint, wrapper success and missing-dependency paths, generation, check-clean, and typecheck passed. Preflight's only failure is disclosed unrelated/uncommitted `.omo` state; it reports no registrar/product/generated drift. | **CLOSED** |

Finding counts: **1 total, 1 ACCEPT, 0 DO NOT ACCEPT, 0 open, 1 closed**.

### Review closure summary

The substantive I, J, K, and close claims re-measure true. Current accounting is 56 tracked/live registrar activations plus 12 live refusals against 68 owed siblings; `10 + 46 + 12 = 68`. The owner record, carriers, oracles, and graph agree on five READY and three DEFINED Specs. Recipe 2 contains the three DEFINED rows; recipe 8 contains the five honest warnings. The frozen K census reproduces byte-for-byte with 12 eligible sessions, zero qualifying episodes, and `STAND-DOWN (unmet)`. Validate remains 156 Specs, 1 Pack, 157 anchors, 314 nodes, 660 edges, 0 errors, and 5 warnings. The focused graph and eight changed self-hosting suites pass. Fresh P37-R1 closure reruns passed lint, the contract-dependent wrapper path, generation, self-hosting check-clean, and typecheck.

The independent review is complete with no open ACCEPT finding. Todo 19 stays unchecked for orchestrator verification. Todo 20 remains separate. This plan remains EXECUTING and makes no final-gate claim.
