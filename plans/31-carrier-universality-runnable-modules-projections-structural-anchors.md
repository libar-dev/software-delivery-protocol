# Plan 31 — Carrier universality, runnable modules, projections, and structural anchors

> **Status:** ✅ EXECUTED
>
> Close: brief A ruled (carrier universality decision + gherkin-authoring follow-through + generated Gherkin read projection); brief B delivered (derived runnable modules, valid-cart mutation log red/red/green); brief C delivered (census + bounded Mermaid with per-diagram refusal); brief D delivered BR1 (component/uses fields, memberOf/uses edges, schema 0.5.0); brief E's re-entry trigger (census + structural edges queryable) is now SATISFIED.
>
> Plan 33 closed the independent review of this execution: graph-valid structural edges now survive
> dangling-target findings without erasing realization, registrar adoption is manifest-owned and
> byte-falsifiable, one scenario invokes the product once with a lazy oracle at the first `Then`,
> every projection publishes diagnostic output on validation errors, and MD-29 through MD-31 plus
> the four promoted behavior Specs are ratified. The fresh plan-33 gate supersedes this plan's
> original close measurement.

This plan commissions briefs **A** (carrier universality), **B** (derived runnable modules),
**C** (census and Mermaid projections), and **D** (structural anchor semantics and edges);
brief **E** (agent command surface) is out and re-enters when **census + structural edges
queryable**. Operational execution is tracked in `.omo/plans/arc-keystone-engines.md`.
