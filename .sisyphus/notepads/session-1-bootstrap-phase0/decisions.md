- Use a source-file shebang for the CLI entry instead of a tsup banner so the library build stays clean; if a banner is ever needed, split the CLI build from the library build.
- Resolve the package name to `src/index.ts` in Vitest via top-level `resolve.alias`; do not rely on `test.alias` for this path.
- Keep the source shebang in `src/cli/sdp.ts` and let tsup post-process the emitted CLI file back to one hashbang line after build, so the source stays executable-friendly without shipping a duplicated shebang.
- Let `examples/**` compile only through `tsconfig.examples.json`, with its own `paths` mapping and an explicit empty `exclude`, so the example can prove the public package alias without loosening the root program.
- Keep `src/index.ts` as a Phase 0 barrel only: export the ID parser/formatter helpers, `ref`, and the branded ID helpers from `src/ids.ts`—nothing model-specific yet.
- Use a tiny `vitest-test.mjs` wrapper so the package can accept `npm test -- --run ids` without double-injecting `--run` while preserving the plain `npm test` behavior.
- Keep the `#` suffix grammar narrow (`<sub>` only, no dotted subpaths) so the ID parser stays aligned with the concept docs and rejects malformed examples early.
- Model the descriptor/section layer as optional, shape-only TypeScript: literal arrays define the canonical values, and section containers stay fully optional so idea-stage fixtures typecheck without readiness-based promotion rules.
- Keep `SpecSections` as an optional interface of optional section payloads, with each payload using a permissive `Record<string, unknown>` shape so the model stays shape-only and lint-clean.
- Extend `src/index.ts` into the Phase 0 builder barrel by exporting the new `spec`/`pack`/relation/anchor helpers alongside IDs and existing schema exports, so Task 4 adds the authored DSL surface without regressing already-present graph-schema typecheck consumers.
- Keep `anchorImplementation()` singular at the builder layer: the identity-only implementation anchor binds one target `SpecId` via `satisfies`, not an array, matching the Task 4/Task 9 contract and avoiding premature many-target semantics in Phase 0.
- Keep the graph schema as a pure, inert module at `src/graph/schema.ts`, and re-export it from the package barrel without adding extractor, writer, or reader behavior.
- Model the Task 6 validation seam as pure authored-layer contracts plus inert readiness-floor data: `AuthoredModel` is only in-memory collections, while graph-shaped `ready` checks stay represented as `deferredInSession1` clauses instead of executable enforcement.
- Keep the CLI stub isolated from the validator/runtime layer: manual `process.argv` parsing only, exact fixed help/error strings, and no filesystem or source-walking imports in `src/cli/sdp.ts`.

- Keep validateAuthoredModel as a pre-graph aggregator over duplicate-id, dangling-reference, and readiness-floor findings, with individual finding families preserved even though the top-level report stays on the conformance surface for Session 1.
