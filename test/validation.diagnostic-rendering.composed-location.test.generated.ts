// GENERATED by `sdp build` — the runnable registrar of spec:validation.diagnostic-rendering.composed-location.
// Derived from the extracted graph (the statically-reified truth, MD-14), keyed by spec ID:
// regenerable, never edited, importable because it is a projection (JS-B2.6's pattern) —
// never the authored spec module (JS-B2.3: no spec↔code import edges).

import type { StepBindings } from "@libar-dev/software-delivery-protocol/runner";
import {
  compareContractOutcome,
  invokeRunnableExample,
  registerRunnableExample,
} from "@libar-dev/software-delivery-protocol/testing";
import type {
  RunnableExampleAdapters,
  RunnableExampleWorld,
} from "@libar-dev/software-delivery-protocol/testing";
import { composedLocationContract } from "../generated/contracts/validation.diagnostic-rendering.composed-location.contract.js";
import type {
  Step,
  StepParams,
} from "../generated/contracts/validation.diagnostic-rendering.composed-location.contract.js";
import type {
  DiagnosticRenderingConditions,
  DiagnosticRenderingOutcome,
} from "../generated/contracts/validation.diagnostic-rendering.space.js";

export function registerComposedLocation<World>(
  adapters: RunnableExampleAdapters<
    World,
    DiagnosticRenderingConditions,
    DiagnosticRenderingOutcome
  >,
): void {
  type Execution = RunnableExampleWorld<
    World,
    DiagnosticRenderingConditions,
    DiagnosticRenderingOutcome
  >;
  const bindings = {
    "a finding naming the validator {validatorId} at severity {severity} carrying the message {message}":
      () => {
        // Given state is materialized by createWorld from the generated point.
      },
    "the {renderer} renderer formats that finding once per location shape": async (execution) => {
      await invokeRunnableExample(execution);
    },
    "the finding carrying the file {file} and the line {line} renders {withLocation}": (
      execution,
      params,
    ) => {
      compareContractOutcome(execution, {
        kind: "then",
        text: "the finding carrying the file {file} and the line {line} renders {withLocation}",
        params,
      });
    },
    "the same finding carrying the file alone renders {fileOnly}": (execution, params) => {
      compareContractOutcome(execution, {
        kind: "then",
        text: "the same finding carrying the file alone renders {fileOnly}",
        params,
      });
    },
    "the same finding carrying neither renders {bare}": (execution, params) => {
      compareContractOutcome(execution, {
        kind: "then",
        text: "the same finding carrying neither renders {bare}",
        params,
      });
    },
  } satisfies StepBindings<Execution, Step, StepParams>;

  registerRunnableExample(
    composedLocationContract,
    {
      validatorId: "honesty/readiness-floor",
      severity: "error",
      message: "The stated rung is not earned.",
      renderer: "command-line",
    },
    ["validatorId", "severity", "message", "renderer"],
    adapters,
    bindings,
  );
}
