// GENERATED by `sdp build` — the runnable registrar of spec:carrier.markdown-pack-authoring.markdown-ts-parity.
// Derived from the extracted graph (the statically-reified truth, MD-14), keyed by spec ID:
// regenerable, never edited, importable because it is a projection (JS-B2.6's pattern) —
// never the authored spec module (JS-B2.3: no spec↔code import edges).

import type { StepBindings } from "@libar-dev/software-delivery-protocol/runner";
import {
  compareContractOutcome,
  invokeRunnableExample,
  registerRunnableExample,
} from "@libar-dev/software-delivery-protocol/testing";
import type {
  RunnableExampleAdapters,
  RunnableExampleWorld,
} from "@libar-dev/software-delivery-protocol/testing";
import { markdownTsParityContract } from "../generated/contracts/carrier.markdown-pack-authoring.markdown-ts-parity.contract.js";
import type {
  Step,
  StepParams,
} from "../generated/contracts/carrier.markdown-pack-authoring.markdown-ts-parity.contract.js";
import type {
  MarkdownPackAuthoringConditions,
  MarkdownPackAuthoringOutcome,
} from "../generated/contracts/carrier.markdown-pack-authoring.space.js";

export function registerMarkdownTsParity<World>(
  adapters: RunnableExampleAdapters<
    World,
    MarkdownPackAuthoringConditions,
    MarkdownPackAuthoringOutcome
  >,
): void {
  type Execution = RunnableExampleWorld<
    World,
    MarkdownPackAuthoringConditions,
    MarkdownPackAuthoringOutcome
  >;
  const bindings = {
    "an extraction root holding the pack carrier {carrierSource}": () => {
      // Given state is materialized by createWorld from the generated point.
    },
    "the extractor derives the graph": async (execution) => {
      await invokeRunnableExample(execution);
    },
    "the graph holds the pack {packId} whose membership names {memberId}": (execution, params) => {
      compareContractOutcome(execution, {
        kind: "then",
        text: "the graph holds the pack {packId} whose membership names {memberId}",
        params,
      });
    },
  } satisfies StepBindings<Execution, Step, StepParams>;

  registerRunnableExample(
    markdownTsParityContract,
    { carrierSource: "the Markdown twin of a TS manifest" },
    ["carrierSource"],
    adapters,
    bindings,
  );
}
