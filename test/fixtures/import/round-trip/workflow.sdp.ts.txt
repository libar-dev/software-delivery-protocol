import { refines, spec, specId } from "@libar-dev/software-delivery-protocol";

export const workflowRoundTripSpec = spec({
  id: specId("spec:round-trip.workflow"),
  title: "Import a workflow",
  kind: "workflow",
  altitude: "story",
  readiness: "idea",
  relations: [refines(specId("spec:round-trip.behavior"))],
  behavior: { flows: ["Reify the source.", "Emit the Markdown twin."] },
});
