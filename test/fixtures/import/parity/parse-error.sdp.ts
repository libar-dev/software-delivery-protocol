import { spec, specId } from "@libar-dev/software-delivery-protocol";

export const parseErrorProbe = spec({
  id: specId("spec:parity.parse-error"),
  kind: "behavior",
  altitude: "story",
  readiness: "idea",
  intent: { outcome: `unterminated
});
