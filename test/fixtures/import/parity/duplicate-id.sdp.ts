import { spec, specId } from "@libar-dev/software-delivery-protocol";

export const duplicateIdProbe = spec({
  id: specId("spec:parity.duplicate-id"),
  kind: "behavior",
  altitude: "story",
  readiness: "idea",
});
