import { spec } from "@libar-dev/software-delivery-protocol";

const id = "spec:parity.non-static-envelope";

export const nonStaticEnvelopeProbe = spec({
  id,
  kind: "behavior",
  altitude: "story",
  readiness: "idea",
});
