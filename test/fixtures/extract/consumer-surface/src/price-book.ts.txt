// A source file the graph records nothing at: no anchor, no binding, no node. It is the
// coverage-unknown case a changeset must name rather than drop into silence.
export const priceBook = new Map<string, number>();
