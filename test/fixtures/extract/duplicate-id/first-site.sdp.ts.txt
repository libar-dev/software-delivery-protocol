import { spec, specId } from "@libar-dev/software-delivery-protocol";

// One of two valid carriers reifying the same fixture-only id.
export const duplicateFirstSite = spec({
  id: specId("spec:fixture.duplicate"),
  title: "TypeScript site of a duplicated id",
  kind: "behavior",
  altitude: "story",
  readiness: "idea",
  intent: { outcome: "Collide with the Markdown carrier's id." },
});
