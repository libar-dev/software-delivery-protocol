
## Todo 1 — phase-plan and gate-ledger scaffolding

- The effort branch was already `feature/protocol-self-application-phase-2`; no branch creation
  was needed and no work was performed on `main`.
- The phase-2 execution plan was absent, so the owner brief supplied its initial content
  than rewriting an existing draft. Its G1-G8 table is explicitly process evidence, never graph
  content.
- `check-self-hosting-gates.mjs` conditionally validates plan 18 only when the plan exists, so
  phase-1's accepted docket and four-gate agreement remain enforced independently.

## Fold-target map correction

- Corrected plan 18's fold-target map to the pinned M8 targets and expanded the coverage ledger
  to the M7 spec IDs. The prior generic `spec:protocol.*` placeholders were planning drift, not
  a new naming ruling.

## Todo 2 — checkout grammar matrix verification

- No §6 ruling was fired: all eleven checkout Spec shapes are covered by the frozen 17b grammar.
  In particular, the single constraint stays within the ruled one-entry form, and the example
  rows use the ruled immediate `gwt` form rather than demanding a new surface.
- The hardening matrix records a named non-claim where Markdown has no executable TypeScript
  analogue; this is evidence discipline, not a new parser or emitter ruling.
