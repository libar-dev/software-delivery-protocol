
## Todo 1 — phase-plan and gate-ledger scaffolding

- The eleven checkout Specs cover all eight `kind` values; the behavior kind appears twice and
  both example rows require the frozen immediate `gwt` placement.
- The hardening appendix is bounded to the ten `extractFindingIds` refusal classes, matching the
  brief's authored-fidelity scope instead of treating all parser diagnostics as parity work.
- The plan-17 docket's review-06 tail has 24 concrete rows; phase 2 carries them as individual
  docket rows plus the four adopted clusters and five named watch items.

## Todo 2 — checkout grammar matrix verification

- The eleven checkout Specs use five of the six ruled relation keys (`refines`, `dependsOn`,
  `constrainedBy`, `decidedBy`, and `verifies`); `supersedes` remains grammar-allowed but is not
  checkout-forced.
- Every observed section maps directly to a frozen 17b owner row. The two example Specs separately
  prove the immediate-after-Intent `gwt` placement, and no checkout shape fires a §6 watch item.
- The hardening appendix can distinguish same-ID Markdown refusals from bounded named non-claims
  rather than overstating parser or executable-expression parity.

## Todo 3 — pure Markdown envelope emitter

- The emitter owns only the fixed envelope and identity prose: relation keys emit in ruled order,
  empty logical relations become `relations: {}`, and title/narrative remain Markdown body content.
- A hand-corrupted emitted relation key is refused at the existing Markdown frontmatter boundary as
  `extract/invalid-frontmatter`; the emitter does not add a second validation path.

## Todo 4 — ruled Markdown section emission

- The emitter can preserve every checkout authored section through Markdown, including the
  immediate example GWT fence and the parent-owned example-space vocabulary fence.
- The one-entry constraints grammar remains refusal-honest: removing `statement` makes the
  Markdown reifier refuse rather than manufacturing a partial constraint.

## Todo 5 — fs-free TypeScript import orchestration

- The import seam consumes one total TS reification, passes its `extract/*` findings through
  unchanged, and adds import-local information without opening a second validation path.
- A module may emit exactly one Spec document while retaining an informational Pack warning; zero
  Specs and multiple Specs remain distinct non-emitting outcomes.

## Todo 6 — import package-barrel surface

- The package root exposes only the pure Markdown emitter and TypeScript import seam; the CLI fs
  layer remains private, and `ImportResult` is available as a type-only barrel export.
- The installed-package smoke fixture is the pinned root-export contract, so its runtime and
  type-only lists must change with the barrel; otherwise the published package correctly rejects
  the new surface even when source-level barrel tests pass.

## Todo 8 — two-layer import round-trip harness

- Authored fidelity is strict `ReifiedSpec.data` equality after TS reification, deterministic
  emission, and Markdown reification; no prose normalization is lawful at this layer.
- Graph comparison strips only carrier suffixes from node files, excludes Pack nodes and their
  edges, compares `schemaVersion`, and separately proves recomputed per-Primitive delivery facts
  under identical copied anchors.
- The corrupted Markdown twin changes a real behavior rule and fails the normalized graph equality,
  proving the harness observes authored prose rather than comparing a tautology.

## Todo 7 — durable CLI import verb

- `sdp import` keeps filesystem work at the CLI boundary: it recursively discovers TypeScript
  carriers, delegates every source to the pure import seam, writes only a missing Markdown sibling,
  and never deletes the source.
- Refusals and existing targets render through the one finding formatter; deterministic filesystem
  failure coverage rides the nested import hook rather than a permission-changing test.

## Todo 11 — installed import package proof

- The existing package-smoke harness is the correct stale-state boundary: it builds, packs, installs
  with `--prefix` into a fresh consumer, and exercises only the installed tarball.
- Root CommonJS consumers require a dedicated `require` export and an `index.cjs` artifact; removing
  `emitMarkdownSpec` from the barrel makes the installed consumer type-surface check fail.

## Todo 9 — import round-trip verifier tracer

- The ready parent owns write-beside, never-delete, refusal-honesty, and single-reifier rules; its
  ready example child binds the authored round-trip harness to the behavior fixture.
- Removing the child's bound GWT while it remains `ready` raises both example evidence floor
  failures. Rewording the parent/child `When` step regenerates the contract and fails the bound
  test at the drifted step, so authored vocabulary changes cannot silently bypass the verifier.

## Todo 10 — exclude diagnostics and matcher boundary

- MD-20 requires Windows drive-letter absolutes to be rejected, not normalized: they cannot be
  root-relative POSIX prefixes and previously became unmatched silent no-ops.
- `normalizeExcludes` owns reusable malformed-path diagnostics while `parseBuildArgs` translates
  them into CLI usage wording, including the exact flag supplied as an `--exclude` operand.
- Prefix matching remains exact-or-slash-descendant; the regression pins that `foo` never excludes
  `foobar`.

## Todo 9 follow-up — import verifier corpus repair

- The frozen self-hosting corpus must advance with both the ready import parent and its ready
  verifier child: 16 Specs, 16 anchors, 33 nodes, and 57 edges.
- Carrier discovery separately pins the enriched parent body, while the self-hosting fixture stays
  byte-identical to the live Markdown carrier.
- The parent derives `has-verifier` through its enabled child; it does not derive `implemented`
  until an anchored `satisfies` binding names the import seam.

## Todo 19 — refusal-parity matrix

- Ten TypeScript refusal classes are covered: six same-class Markdown findings and four named non-claims. Root CLI views exclude the intentionally malformed parity fixtures.

## Todo 20 — parser hardening claim

- The settled matrix supports a bounded like-for-like refusal-parity claim only: six classes share finding IDs, while four classes remain explicit named non-claims rather than a full-parity assertion.

## Todo 21 — Gate G3 close

- G3 was accepted at `0bb200a` after the hardening baseline's parity matrix and parser-Spec disposition passed the full green gate.
- `check-temporal.mjs` now excludes `.omo/evidence/` alongside `.omo/plans/`, so temporal-by-genre evidence done-records do not block the self-hosting gate.

## Todo 22 — canonical-default carrier flip

- The flip landed as one atomic operative-record change: Markdown is the default Spec carrier, Packs remain TS pending a Pack syntax ruling, and the TS DSL remains both import source and lawful per-ID option.
- `check-carrier-rule.mjs`, `check-carrier-truth.mjs`, `check-self-hosting-gates.mjs`, the anti-misleading sweep, and `npm run check` all passed; the truth audit now pins the `sdp import` row's `many source adapters` amendment.

## Todo 23 — clean-clone flip verification

- A pristine clone of `feature/protocol-self-application-phase-2` at `dd19060` passed `npm ci && npm run check`; its full output is recorded in the task-23 clean-clone log.
- No non-Pack `.sdp.ts` carrier remains under `specs/` or `examples/checkout-v1/specs/`; the two lawful Pack manifests and both duplicate-ID fixture carrier surfaces remain. Deleting the TypeScript fixture twin makes the fixture-intact assertion fail and restoring it returns the assertion to green.
- The post-flip focused package smoke and an explicit packed-tarball consumer both exposed `sdp import --dry-run` and the package barrel; acceptance 1–3 and 8 evidence is assembled in `task-23-self-hosting-phase-2.acceptance.md`.

## Todo 24 — Gate G4 re-presentation pending

- The owner rejected the pre-written G4 acceptance: todo 22(f) had not repaired `04` §1, and the ledger stamp preceded the owner's disposition.
- The amended flip `0a06882` makes the canonical-surface config explicitly designed-for and deferred (**ASPIRATIONAL**) and names file-existence-only as the current realization.
- G4 remains pending owner acceptance; `check-carrier-rule.mjs`, `check-carrier-truth.mjs`, `check-self-hosting-gates.mjs`, and `npm run check` all passed after the amend.

## Todo 25 — corpus wave 1a model subsystem

- Wave 1a adds six defined model-subsystem Specs: the core primitive and descriptors, sections,
  relations, stable IDs, Pack aggregate, and generic source anchors. `spec:model.core-model` is a
  genuine shared vocabulary carrier, so the self-hosting Pack names it in `modelRefs`.
- Each realization claim uses a binding-only code anchor at the named implementation surface; the
  section and anchor Specs each use a second anchor for their readiness-floor and extraction seams.
  No wave-1a Spec claims a verifier or `ready` state.
- G4 remains pending and its ledger row is unchanged. The OMO continuation directive and the
  explicit user `continue` instruction authorize this wave despite the pending owner acceptance.

## Todo 26 — corpus wave 1b validation and consumers

- Wave 1b adds four defined Specs: the two check families, the projections model, the agent
  surface, and Design Review. Their rule/model evidence clears `defined`; none asserts `ready` or
  a verifier.
- The four realization bindings are deliberately narrow: `graphValidatorIds` for validation,
  `DesignReviewPage` for the projection contract, `createReader` for the agent surface, and
  `renderDesignReview` for the human projection. The projections model uses its own page-contract
  binding so the Design Review anchor remains specific to the flagship behavior.
- The generalized context-efficiency statement belongs to the agent-surface behavior as an
  empirical comparison claim; the measurement figure and its disposition remain fold work.
