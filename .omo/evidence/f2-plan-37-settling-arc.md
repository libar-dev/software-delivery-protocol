# Plan 37 F2 code quality review

Reviewer: omo senpi-task child `st_01a01e48`, independent of the arc authors.
Worktree: `/home/darkomijic/dev-libar/software-delivery-protocol`
Branch: `feature/sdp-skills`
HEAD reviewed: `a4a1468f59d199a1d381b23837ebcda9488f8614`

## Verdict

`APPROVE`

Zero open severity findings. Named gates were clean. I would ship this delta as test and config quality.

## Baseline and scope

Plan 37 commission parent is `a8d5898f549778d5841653dc81730a0c5810e446` (Merge PR #18). Commission commit is `1dae853a6f51bdcb8c4bde16d84dafda611e0fca`. Reviewed head is `a4a1468`.

The first-parent merge `7b99baa` is PR #21 (`feature/adoption-maturation-arc`) onto that same parent. Second parent is `dfb899b`. Product and test changes in `a8d5898..a4a1468` are Plan 37, not a foreign arc.

Inspected every product, test, and config path in that range. No `src/` diff. No `package.json`, `tsconfig.json`, `vitest.config.ts`, `test/helpers/`, or `docs/agent-surface/recipes.md` diff. The ten first-tranche generated registrars are byte-untouched. Generated registrar files in the range are adds only. `git log --diff-filter=M a8d5898..a4a1468 -- 'test/*.test.generated.ts'` is empty.

Current tracked dirt is `.omo/boulder.json` and checkbox-only `.omo/plans/plan-37-settling-arc.md`. That is orchestration state, not product. Untracked `sdp-skills-gen1-parity*` and the ultrawork note are a paused sibling plan. Untracked F1/F3 evidence and the Todo 19 verifier are concurrent verification, unread for this verdict.

## Findings

None.

## Special attention

### 46 later adopted registrars

`git ls-files -- 'test/*.test.generated.ts'` is 56. First-tranche 10 plus later 46. Each later file starts with `// GENERATED by \`sdp build\``, exports `register…`, and calls `registerRunnableExample`. No TODO, eslint-disable, or `@ts-` in those 46 files. Authored suites import the generated `register*` symbols and keep `specTest` anchors.

Adoption counts match the live `bindExample(` census:

| Suite | Adopted `register*` | Remaining `bindExample(` |
| --- | --- | --- |
| `test/self-hosting-carrier.test.ts` | 1 (plus 2 first-tranche) | 0 |
| `test/self-hosting-validators.test.ts` | 16 (plus 3 first-tranche) | 0 |
| `test/self-hosting-carrier-gherkin.test.ts` | 11 | 2 |
| `test/self-hosting-pack-markdown.test.ts` | 2 | 0 |
| `test/self-hosting-sdp-import.test.ts` | 1 | 0 |
| `test/self-hosting-projections.test.ts` | 10 | 1 |
| `test/self-hosting-extraction.test.ts` | 5 | 4 |
| `test/self-hosting-consumers.test.ts` | 0 (file untouched) | 5 |

46 + 12 = 58 deferred sites. Live `bindExample(` total is 12.

Anchor oracle `test/self-hosting-oracle/anchors.ts` changes 46 `site:` strings and nothing else. Register pins 56, bindExample pins 12, other sites 78, 146 site fields on 157 anchors (some anchors are not example sites). Refused pins still name the live `bindExample(…Contract` tokens.

### 12 lawful refusals

All twelve remain on `bindExample` with the original multi-call or partial-point bindings.

- Gherkin `contractParityContract`, `unboundReadyRefusedContract` in `test/self-hosting-carrier-gherkin.test.ts`
- Projection `boundSpecPageContract` in `test/self-hosting-projections.test.ts` (comment: sibling requires unused `packId`, and the comment does not contain `bindExample(`)
- Extraction `concretenessRefusalContract`, `multiEntryExampleContract`, `caseCollidingPathContract`, `stepOrderContract` in `test/self-hosting-extraction.test.ts`
- Consumers five sites unchanged in `test/self-hosting-consumers.test.ts`

I did not treat refusal as a quality defect. The frozen adapter surface is the reason, and the authored expects on those paths are still there.

### `contract-dependent-suites.mjs`

Root `testPaths` is 71 exact strings, no globs, no `src/` entries, no duplicates. Generated subset is 56 and equals `git ls-files -- 'test/*.test.generated.ts'` exactly. The other 15 paths are the pre-existing authored suites plus `test/helpers/generated-contract.ts`. P37-R1 added the 46 later siblings and alphabetized `test/self-hosting-carrier.test.ts`. `vitest-test.mjs` and `eslint.config.js` both read this module.

### P37-R1 lint exception

`eslint.config.js` adds only `@typescript-eslint/no-redundant-type-constituents: "off"`. It sits in the existing override whose `files` is `[...rootContractDependentSuite.testPaths]`. That list is exact paths. The rule cannot fire-suppress `src/`, `test/self-hosting-graph.test.ts`, or any file not named in the row. The same override already disabled the `no-unsafe-*` family for clean-clone lint before generation. Typecheck is unchanged: `tsc --noEmit -p tsconfig.json` still type-checks `test/**/*.ts`, including the generated registrars, against real contract modules. `no-redundant-type-constituents` appears nowhere else.

### Test comments and census safety

Commit `4851740` rewrote the bound-spec-page comment from `// bindExample(boundSpecPageContract — …` to `// Refused: generated sibling requires unused packId.` A `bindExample(` grep no longer hits that comment. A scan of `test/self-hosting-*.test.ts` found no comment containing `bindExample(`. Live census is 12, matching the 12 refusals.

### Readiness oracle

Five carrier frontmatter rungs `defined` → `ready` in the owner-ratified set, and the matching descriptor rungs in `test/self-hosting-oracle/{carrier,extraction,model}.ts`. Untouched: `projections-model`, `regenerability`, `core-model`.

`test/self-hosting-oracle/index.ts` replaced `expectedWarnings = []` with five full objects (`validatorId`, `family`, `severity`, `subjectId`). `test/self-hosting-graph.test.ts` still does `.toEqual(expectedWarnings)` on that projection, not a count. Histogram only: `{ defined: 9, idea: 3, ready: 143, scoped: 1 }`. Structural pins stay `156 / 1 / 157 → 314 / 660`.

### Discovery pin

`test/check-self-hosting-gates.test.ts` now expects `number: 37` and `37-adoption-tranches-drift-maturation-and-bundle-measurement.md`. The rest of that file is the existing selector and ledger probes.

## Authored-suite quality

Adapters follow the already-shipped first-tranche shape: `createWorld` materializes the point, `invoke` is one product call, `observe`/`expected` own the comparator Then, extra Thens live in `assertions` via `paramsForStep`. No `it.skip` / `describe.skip` / `it.only`. No `sleep`, timers, `Date.now`, or `Math.random` in the changed suites. The one `eslint-disable-next-line` in `test/self-hosting-extraction.test.ts:608` dates to `cee0cf1` (2026-07-26), not this arc.

Expect-line churn is renaming `params.x` to contract-derived names, or moving the primary Then into `observe`/`expected`. I did not find a contract Then that disappeared.

Two non-findings I almost filed and then dropped:

1. `test/self-hosting-extraction.test.ts` same-invocation When used to assert `{ exitCode: 0, stderr: "" }` inside the CLI call. Invoke now parses stdout and the contract Then still locks `exitCode`. Empty stderr was never a Then. A noisy-but-zero-exit regression would no longer fail. That is When-step hygiene, not a lost example law.
2. `test/self-hosting-sdp-import.test.ts` dropped `expect(world.relativePath).toBe("behavior.sdp.ts")`. `createWorld` hardcodes that path. The round-trip assertions remain.

`test/self-hosting-graph.test.ts` still titles the warning test "leave without a finding" while asserting the five honesty objects. The title is stale. The assertion is the exact oracle. I will not fail a quality review on a describe string.

Validators (1187 pure LOC) and projections (1084) are over the 250-line source ceiling. The plan's write unit is the suite file, so splitting them would have been a scope breach. Anchors.ts is a 157-row data table.

## Named gates

Serialized in this task. No generation. No `npm run check`. Generated contracts were already present (`generated/contracts` has 102 files). I did not regenerate them.

| Gate | Command | Result |
| --- | --- | --- |
| lint | `npm run lint` | exit 0 |
| typecheck | `npm run typecheck` | exit 0 |
| focused authored suites | `npx vitest run` on the nine changed files below | 9 files, 90 tests, pass, 3.44s |

Files: `test/check-self-hosting-gates.test.ts`, `test/self-hosting-carrier.test.ts`, `test/self-hosting-carrier-gherkin.test.ts`, `test/self-hosting-extraction.test.ts`, `test/self-hosting-graph.test.ts`, `test/self-hosting-pack-markdown.test.ts`, `test/self-hosting-projections.test.ts`, `test/self-hosting-sdp-import.test.ts`, `test/self-hosting-validators.test.ts`.

A first attempt to drive the same list through `node ./vitest-test.mjs` died with `spawnSync vitest ENOENT` because that wrapper expects `node_modules/.bin` on `PATH`. That is how `npm test` runs, not how a bare `node` run works, and it is pre-existing. Tests had not started. The `npx vitest run` invocation above is the plan's lane-safe command and the gate I am signing.

## Unrelated state, left alone

- Dirty `.omo/boulder.json` adds paused work `sdp-skills-gen1-parity` and an extra Plan 37 session id.
- Dirty `.omo/plans/plan-37-settling-arc.md` checks Todo 19 and F3. I did not write that file and did not mark F2.
- Untracked parity drafts/plans/evidence, ultrawork note, F1/F3 evidence, Todo 19 verifier.

No product, test, config, plan, generated, ledger, or Boulder file was edited by this review. No process remains.
