# Ultrawork Notepad — Complete Plan 37 and sdp-skills-gen1-parity
Started: 2026-08-20T08:13:46+02:00

## Plan (exhaustively detailed)

Tier: LIGHT. This root session is control-plane only under `start-work`: workers own every repository edit, test, QA run, review, commit, and PR action. Root touches only `.omo/` orchestration state, evidence routing, plan checkboxes, and verdicts.

Skills:

- `start-work`: binding workflow because the user asked to complete two Prometheus plans. Root never implements or runs QA.
- `unslop`: binding for every skill, README, Spec, plan, PR, and evidence prose change.
- `coding-agent-sessions`: required for Plan 37 Brief K’s session census. The Todo 17 worker must load and follow it.
- `sdp-agent-surface`: required by workers that query the graph or verify corpus/readiness facts.
- `sdp-authoring`: required by the Todo 16 worker because readiness edits are authored Spec changes.
- `git-master`: required by Plan 37 close/PR workers because commits and PR state are part of that plan.
- `memory-discipline`: consulted now. This run’s facts are project execution state, so they belong in `.omo/` evidence and the ledger, not durable personal memory.

Delegation topology:

1. Resolve the sole owner-only fork for Todo 16: eight READY/DEFINED ratifications. No worker may invent this human statement.
2. After ratification, dispatch Plan 37 Todo 16 to one authoring worker, then an independent verifier. It owns the eight carrier/oracle readiness dispositions and its evidence.
3. Dispatch Plan 37 Todo 17 to one session-census worker loading `coding-agent-sessions`; independently verify the frozen measurement and verdict.
4. Dispatch Todo 18 to one close-record worker after 16 and 17. It owns the I/J/K execution record and fresh graph measurements; independently verify.
5. Dispatch Todo 19 to one adversarial review worker after 18. Loop any criterion-cited failure back to its owning worker.
6. Re-audit already checked Todo 20 and F1-F5 in parallel after 19 because the user explicitly named them and their evidence predates the remaining close work. Separate reviewers own disjoint evidence artifacts; root integrates verdicts only.
7. Close Plan 37: primary record and `AGENTS.md` must say EXECUTED, all Plan-37 top-level boxes must be checked, the full gate must be green, PR/delivery state must satisfy the plan, and Boulder/ledger state must be coherent.
8. Re-run parity Todo 1 with a fresh executor and independent verifier. Release Wave 1 only if both required status sources say EXECUTED, the self-hosting gate exits 0, and there is no overlap on the three parity files.
9. Dispatch parity Todos 2, 3, and 4 in one parallel batch: graph-read skill, authoring skill, and README have disjoint file ownership. Each worker writes its evidence and runs its focused checks/real CLI scenario. No commits are authorized.
10. Independently verify each Wave 1 DoneClaim. Loop any failure back to the same executor before closing its checkbox.
11. Dispatch parity Todo 5 as the serialized merged-tree gate: focused tests, `npm run check`, package dry-run, real CLI help/success/console/error probes, changed-file audit, and cleanup.
12. Dispatch parity F1-F4 as four fresh parallel reviewers. All must approve; any non-approval returns to the owning executor.
13. Close parity Boulder/ledger/plan state, audit every todo and cleanup receipt, then complete the registered goal.

## Success criteria + QA scenarios

### Criterion 1 — Plan 37 is truly closed

Pre-change RED surface: both live status sources say `EXECUTING`, and the fresh Plan-37 checkbox census has open rows 16-19. These are authored-intent/evidence changes, so sentence-pinning tests are forbidden; the live status/graph/checkbox surfaces are the lawful failing scenario.

Exact GREEN scenario:

```text
npm run check
node check-self-hosting-gates.mjs .
```

Also read the exact status lines in `plans/37-adoption-tranches-drift-maturation-and-bundle-measurement.md` and `AGENTS.md`, and count column-zero checkboxes in `.omo/plans/plan-37-settling-arc.md`.

PASS iff both commands exit 0, gate stderr is empty, `currentRecord.status` is `EXECUTED`, both source lines say EXECUTED, and the Plan-37 open-checkbox count is zero. Evidence: the Plan-37 task/final artifacts named by the plan plus `.omo/start-work/ledger.jsonl`.

### Criterion 2 — parity guidance and package surfaces match the plan

Pre-change RED surface: parity Todo 1 is blocked because Plan 37 is not closed. Pure-prose skill and README corrections have no lawful sentence-pinning RED seam. Existing machine-consumed contract tests must remain unchanged.

Exact GREEN scenarios:

```text
npx vitest run test/skills.test.ts test/recipes.test.ts test/cli-q.test.ts test/package-smoke.test.ts
npm run check
npm pack --dry-run --json
pnpm --silent sdp --help
pnpm --silent sdp:q 'return { specs: g.specs().length, packs: g.packs().length }' --json
pnpm --silent sdp:q 'console.log("side-output"); return "value"'
pnpm --silent sdp:q 'throw new Error("qa-sentinel")'
```

PASS iff focused/full gates exit 0; package output contains exactly the same three skill files and recipe catalog with no fourth skill/reference tree; help/success/console output matches the guidance; the throwing query exits non-zero; parity’s changed-file set is exactly the two skill files and README; all nine parity boxes are checked. Evidence: `.omo/evidence/task-1..5-sdp-skills-gen1-parity.md` and `.omo/evidence/final-1..4-sdp-skills-gen1-parity.md`.

### Criterion 3 — independent verification and cleanup are complete

Exact scenario:

```text
git status --short
git worktree list --porcelain
```

Then audit the plan files, Boulder work records, ledger, evidence artifacts, active children, monitors, temporary paths, and QA resources.

PASS iff every worker DoneClaim is independently confirmed; Plan 37’s refreshed close reviewers and parity F1-F4 all approve; no live child/monitor/process/temp worktree/scratch artifact remains; cleanup receipts are present; Boulder marks both works completed; no unauthorized commit exists for parity.

WHEN TO STOP: I’ll stop right away when both plans have zero open top-level checkboxes, all named commands and real CLI scenarios pass with captured artifacts, every independent verifier approves, cleanup receipts are complete, and Boulder/goal state is closed.

## Now

Bootstrap complete through discovery. The current step is obtaining the eight owner READY/DEFINED statements required by Plan 37 Todo 16.

## Todo

1. Obtain the eight Brief J owner ratifications for Plan 37 Todo 16.
2. Execute and independently verify Plan 37 Todo 16.
3. Execute and independently verify Plan 37 Todo 17.
4. Execute and independently verify Plan 37 Todo 18.
5. Execute and independently verify Plan 37 Todo 19.
6. Re-audit Plan 37 Todo 20 and F1-F5 after close work.
7. Close Plan 37 and prove EXECUTED status.
8. Re-run and verify parity Todo 1.
9. Execute and verify parity Todos 2-4 in parallel.
10. Execute parity Todo 5 merged gates.
11. Execute parity F1-F4 in parallel.
12. Close both work records and complete the goal.

## Findings

- Fresh repository state is branch `feature/sdp-skills` at merge commit `7b99baa`; PR 21 is already merged into this branch’s history.
- Plan 37’s fresh open-box census is four rows: 16, 17, 18, and 19. Todo 20 and F1-F5 are checked but must be re-audited because the user named them and their evidence predates the remaining close work.
- Plan 37 remains `EXECUTING` in the primary record.
- The owner bundle explicitly says it does not ratify or recommend. Five READY candidates, two DEFINED candidates, and one unselected alternative are prepared, but every owner field is pending.
- The previous goal remains tool-marked `blocked`. `create_goal` was attempted as required and refused because an unfinished goal already exists. The user’s new message explicitly resumes and expands that same goal; execution continues against the existing goal contract plus this notepad.
- Current parity scope is clean; only `.omo/` plan/evidence/Boulder state is dirty.

## Learnings

- `node check-self-hosting-gates.mjs .` accepts a consistent EXECUTING status. Exit 0 alone never proves Plan 37 is closed.
- A prepared readiness candidate is not an owner statement. Do not promote Specs by inference.

## Transition — 2026-08-20T08:14:00+02:00

Sized at 22 live orchestration tasks across eight phases. Fan-out is intentionally deferred at the owner gate: Plan 37 Todo 16 blocks every later Plan-37 row, and parity remains blocked on Plan 37. Once released, disjoint final-review and parity-writing lanes fan out in parallel; sequential Plan-37 rows stay single-worker because each consumes the prior row’s artifact.

The user explicitly resumed both plans, but Todo 16 still requires a human owner statement for every Spec. Evidence-backed proposed set:

```text
spec:model.core-model: DEFINED — blocking enrichment-lifecycle question remains open
spec:model.pack-aggregate: READY — no blocking Pack contract found
spec:model.relations: READY — complete typed relation vocabulary; no blocker found
spec:model.spec-sections: READY — strongest packet; no blocker found
spec:extraction.claim-taxonomy: READY — complete bounded claims glossary; no blocker found
spec:extraction.regenerability: DEFINED — quoted thresholds lack their measurement artifact
spec:consumers.projections-model: DEFINED — impact-graph/measurement work remains unlanded and plan 35 explicitly retained defined
spec:carrier.markdown-authoring: READY — complete invariant with carrier surface refined by children; no blocker found
```

No worker is dispatched until the owner ratifies or changes this set.

## Transition — owner gate recheck

Correction: the first discovery read was truncated at the tool’s 50 KB limit and undercounted open Plan-37 rows. The full plan read confirms all ten expected rows remain open: Todos 16–20 and F1–F5. The execution order is unchanged.

The continuation contained no owner READY/DEFINED statement. Todo 16 remains lawfully blocked; no child, edit, test, commit, or PR action started.

## Transition — owner ratification received

Owner statement: `Ratify proposed set`.

Ratification recorded in `.omo/evidence/plan-37-j-packets/RATIFICATION-BUNDLE.md`: five READY (`pack-aggregate`, `relations`, `spec-sections`, `claim-taxonomy`, `markdown-authoring`) and three DEFINED (`core-model`, `regenerability`, `projections-model`). Todo 16 is released.

## Now

Dispatch Plan 37 Todo 16 to apply only the five ratified one-rung changes, update matching oracle descriptors, prove the three refused alarms remain, and commit the verified unit without absorbing unrelated `.omo/` state.

## Transition — Todo 16 blocked before commit

Executor `st_01a01dd3` applied the five ratified rungs and matching family descriptors. Observable behavior is correct: recipe 2 moved from eight alarms to exactly the three DEFINED Specs, validation is clean, and structural counts stayed `156/1/157 -> 314/660`.

The required graph test is red for two deterministic truth-oracle reasons:

- frozen readiness distribution still expects `defined: 14, ready: 138`; live truth is `defined: 9, ready: 143`;
- global expected warnings remain empty, while the five READY Specs without resolving verifiers now honestly emit five `honesty/gaps` warnings.

No commit exists. A fresh deep adjudicator now decides whether these two minimal expectation updates are lawful derived-truth maintenance under Todo 16, or whether the plan must be amended by the owner first. No executor may touch tests until that verdict.

## Transition — scope adjudication complete

Adjudicator `st_01a01dda` returned `PLAN_AMENDMENT_REQUIRED` with high confidence. History proves both expected-value updates are normal derived-truth maintenance, but the current Plan-37 freeze and single-writer language forbid them.

Requested owner amendment is intentionally narrow:

- allow Todo 16 to update only `test/self-hosting-graph.test.ts:197` from `{ defined: 14, ready: 138 }` to `{ defined: 9, ready: 143 }`, preserving exact histogram assertions;
- allow Todo 16 to update only `test/self-hosting-oracle/index.ts:52-55` from no warnings to the exact five `honesty/gaps` warning objects;
- retain all structural count pins at `test/self-hosting-graph.test.ts:142-147`;
- retain every validator, floor, check implementation, test precision, and other test/oracle freeze.

No test edit is authorized until the owner approves this amendment.

## Transition — narrow amendment approved

Owner statement: `yes`, approving the immediately preceding narrow amendment request.

The Plan-37 scope, single-writer rule, Todo 16 work, acceptance criteria, and failure scenario now authorize only:

- the exact readiness histogram update;
- the exact five-warning oracle update.

Structural pins, validator/floor/check code, every other test/oracle, and assertion precision remain frozen. Executor `st_01a01dd3` may resume.

## Transition — Todo 16 DoneClaim

Executor `st_01a01dd3` committed `7f768d1679d3c78e57aef86638609b372f8dc5a0` (`docs(specs): state ready on owner-ratified drift-alarm Specs (brief J)`).

Claimed evidence: focused graph test 26/26; canonical validation 0 errors with five expected warnings; recipe 2 exactly the three DEFINED alarms; typecheck, format, and diff checks green; unrelated `.omo/` dirt preserved. Todo 16 remains in progress until a fresh verifier confirms the commit.

## Transition — Todo 16 verification epoch 1

Verifier `st_01a01de7` confirmed the commit manifest, exact owner decisions, precise two-file expectation update, structural-pin preservation, focused test 26/26, canonical validation, exact five warnings, exact three recipe-2 alarms, typecheck, format, diff, generated-state cleanup, and dirty-worktree isolation.

It returned `needs-human-review` only because its own auxiliary commands failed: unsupported `g.spec`, an incorrectly ordered `check:self-hosting` before generation later corrected to green, invalid `awk` quoting later corrected, and a shell lookup for an `apply_patch` executable instead of the provided tool. None cites a Plan-37 criterion or source defect.

The same verifier is being re-run with the acceptance boundary explicit. No implementation delta is allowed or needed.

## Transition — Todo 16 FullyDone

Same-context re-review returned `confirmed`. Todo 16 is complete at commit `7f768d1679d3c78e57aef86638609b372f8dc5a0`; all Plan-37 criteria, adversarial probes, and cleanup are satisfied. The plan checkbox is closed.

## Now

Dispatch Plan 37 Todo 17: reconstruct the eligible I/J session corpus, run the frozen Brief K census, adjudicate co-use, and write a re-runnable COMMISSION or STAND-DOWN verdict. No product surface may change.

## Transition — Todo 17 DoneClaim

Executor `st_01a01df5` reports `STAND-DOWN (unmet)` on an adequate 12-session corpus: 8 Brief I, 4 Brief J, zero qualifying episodes, and no shared recipe core.

Claimed evidence includes frozen-tool self-check, byte-identical plan-35 validation, complete candidate inventory, byte-identical same-inventory rerun (`sha256 1ea79c...de66`), explicit missing-record failure, malformed-line accounting, scope allowlist, and frozen-artifact integrity. Todo 17 remains in progress pending independent transcript and arithmetic verification.

## Transition — Todo 17 FullyDone

Verifier `st_01a01e00` independently reconstructed the corpus and confirmed `STAND-DOWN (unmet)` at 0.99 confidence: 12 eligible sessions (8 I, 4 J), zero qualifying episodes, empty shared core, deterministic census hash `1ea79c...de66`, and all failure probes/cleanup passed.

## Now

Dispatch Todo 18 to assemble the Plan-37 close record from the verified I ledger, owner-ratified J table, and Brief K stand-down, then re-derive every close measurement twice and create the authorized close-record commit.

## Transition — Todo 18 DoneClaim

Executor `st_01a01e0a` committed `8e6a86bf946dd28958a01d5034ce849d97d88731` with the I ledger, J table, K stand-down, and two-pass close measurements. Claimed reconciliation is 58 deferred sites = 46 adopted + 12 refused; total owed 68 = 10 first tranche + 46 later adopted + 12 refusals.

The executor also checked the Todo 18 plan box inside the commit before independent verification. The todo remains in progress here; a fresh verifier must confirm the content and explicitly adjudicate that premature checkbox transition before Todo 19 can start.

## Transition — Todo 18 FullyDone

Verifier `st_01a01e1d` confirmed the close record, every live I row, five READY and three DEFINED J dispositions, Brief K stand-down, all fresh measurements, and `10 + 46 + 12 = 68`. Status remains EXECUTING and Todo 20 is not claimed green.

The premature checkbox is retained as a process finding; independent confirmation now makes its state true.

## Now

Dispatch Todo 19 to perform the Plan-32-mold adversarial review over the full Plan-37 delta. A real finding requiring source change blocks the review-register commit and returns to an executor; false or out-of-scope findings need explicit DO NOT ACCEPT evidence.

## Transition — Todo 19 finding P37-R1

Reviewer `st_01a01e27` found one MEDIUM, ACCEPT, OPEN scope finding:

- commit `4851740` added all later-tranche generated suites to `contract-dependent-suites.mjs` and disabled `@typescript-eslint/no-redundant-type-constituents` only in the contract-dependent-suite ESLint override;
- the changes are technically required for clean-clone lint/check coverage of the 56 tracked registrars;
- Plan 37 nevertheless says no check changes and contains no Brief-I exception, so usefulness does not make them authorized.

Recommended owner disposition: ratify a bounded Brief-I dependency-inventory exception for those two existing changes only. Keep every other check/config surface frozen and retain later typecheck enforcement. After approval, the same reviewer must re-run lint, the contract-dependent wrapper path, `check:self-hosting`, and attributable preflight, then close P37-R1 and create the Todo 19 review commit.

Todo 19 and Todo 20 remain blocked pending the owner statement.

## Transition — P37-R1 owner disposition

Owner statement (2026-08-20): `Approve Brief-I exception`.

The bounded exception now authorizes only the existing clean-clone dependency inventory for the exact 56 tracked generated registrar siblings and the existing contract-dependent-suite-only redundant-type-constituents lint exemption. Later typecheck enforcement remains intact; every other check/config surface remains frozen.

The original reviewer must now re-run the named closure gates and close or re-open P37-R1 from current bytes.

## Transition — Todo 19 DoneClaim

Reviewer `st_01a01e27` closed P37-R1 after owner ratification and committed the full Plan-32-mold register as `a4a1468f59d199a1d381b23837ebcda9488f8614`.

Claimed closure: one ACCEPT finding, one CLOSED, zero open; 56 tracked registrar siblings exactly match the dependency inventory; the ESLint exception is confined to the contract-dependent override; base lint/typecheck enforcement remains; no other check/config implementation change exists.

Todo 19 remains in progress until a fresh verifier confirms the commit, bounded exception, review register, gates, status boundary, and scope.

## Transition — Todo 19 FullyDone

Verifier `st_01a01e3b` confirmed commit `a4a1468f59d199a1d381b23837ebcda9488f8614`, its exact four-file manifest, P37-R1's one ACCEPT/CLOSED disposition, 56=56 dependency inventory, bounded lint override, later typecheck enforcement, fresh closure gates, verified-row regression, and untouched EXECUTING status.

Todo 19 is checked. F1-F4 now run in parallel as the explicit dependencies of Todo 20. Todo 20 remains blocked until all four APPROVE verdicts are surfaced and the owner gives the required final-wave okay.

## Transition — F1 REJECT

F1 reviewer `st_01a01e47` rejected on one historical commit-boundary defect:

- commission commit `1dae853` added Todo 2's generated `bounded-parity` sibling despite Todo 1's suite prohibition;
- Todo 2 commit `e081071` contains the authored suite and evidence but not that sibling;
- delivered behavior, byte state, graph facts, all other Todos 1-19, P37-R1 closure, and pre-close status passed.

No history rewrite or silent exception is authorized. Todo 20 remains blocked while F2-F4 finish independently; the combined final-wave record will determine whether this needs a bounded owner disposition or another lawful correction.

## Transition — F3 APPROVE

F3 operator `st_01a01e49` approved after real-surface use: CLI help; graph-query happy path; bounded unknown-command and invalid-body failures; adopted registrar execution 3/3; clean-clone missing-contract refusal with exact recovery command; live incomplete-point refusal before the oracle; current 156-Spec / five-warning / zero-backlog / three-alarm / no-bundle state; and unchanged EXECUTING status.

F3 is checked. F1 remains rejected; F2 and F4 remain active; Todo 20 remains blocked.

## Transition — F4 REJECT

F4 auditor `st_01a01e4a` confirmed the final tree and every product-scope guardrail, but rejected historical commit-boundary fidelity:

- F4-B1 repeats F1: Todo 2's generated tracer sibling landed in commission commit `1dae853`, while activation landed in `e081071`;
- F4-B2: tracer, Wave-1, and Wave-2 shared anchor-pin batches landed as standalone commits `4a451e2`, `0b098a3`, and `07098f3`, despite their `Commit: N` instruction to ride adoption commits.

These are history-only process defects; current bytes, ownership, tests, forbidden surfaces, and status boundaries pass. No destructive rewrite or retrospective exception is authorized yet. F2 remains active, and Todo 20 remains blocked.

## Transition — F2 APPROVE

F2 reviewer `st_01a01e48` approved with zero findings. Lint, typecheck, and nine changed authored suites (90 tests) passed once. The 46 adopted registrars, 12 refusals, exact dependency inventory, bounded lint exception, readiness oracle, discovery pin, deterministic tests, and unchanged frozen surfaces all passed quality review.

F2 is checked. F1 and F4 remain rejected only on the historical commit-boundary deviations; F3 remains approved. Todo 20 remains blocked pending an owner disposition and reviewer re-audit.

## Transition — historical boundary owner disposition

Owner statement (2026-08-20): `pprove historical boundary deviations`, unambiguously accepting the surfaced `Approve historical boundary deviations` choice.

Accepted deviations, without history rewrite:

1. Todo 2's generated `bounded-parity` sibling landed in commission commit `1dae853`; its authored activation and evidence landed in `e081071`.
2. Tracer, Wave-1, and Wave-2 anchor-pin batches landed in standalone commits `4a451e2`, `0b098a3`, and `07098f3` instead of riding the adoption commits named by their `Commit: N` instructions.

This disposition does not claim those original boundaries were met. It records the deviations as accepted because current bytes, ownership, product scope, byte gates, quality review, and manual QA are clean. No other commit-strategy exception is authorized.

## Transition — historical deviation record committed

Writer `st_01a01e55` committed `28ec9d133999faf6c0aa52679c8aedff0a10c409` with the exact eight-path record: F1-F4 evidence, Todo-19 verification, the operational and primary Plan-37 disposition, and the dedicated owner-disposition evidence.

The original commit strategy remains visible; the record says it was not met, no history rewrite occurred, and no other exception is authorized. F1/F4 remain rejected and unchecked until their original reviewers re-audit this commit.

## Transition — F1 APPROVE after re-audit

Original F1 reviewer `st_01a01e47` re-audited commit `28ec9d1` and approved with zero open findings. The original rejection remains in its artifact; the exact historical defect is ACCEPTED/CLOSED by the owner disposition, not erased or relabeled compliant.

F1 is checked. F2/F3 remain approved. F4 remains active and unchecked; Todo 20 remains blocked.

## Transition — F4 re-audit REJECT, new F4-B3

Original F4 reviewer `st_01a01e4a` closed F4-B1/B2 under the exact owner disposition, then found a third unaccepted historical placement defect:

- Todo 4 says its measurement tooling is `Commit: N` and will be committed at close with the K record;
- dedicated Wave-0 commit `c265f2d` instead lands the definition, census tooling, captures, task record, and operational-plan change;
- close commit `8e6a86b` does not contain those tooling files.

F4's first audit had marked `c265f2d` pass, so no further owner disposition will be requested until a fresh auditor exhaustively maps every `Commit: Y/N`, wave ride-along, evidence checkpoint, and actual manifest. Todo 20 remains blocked.

## Transition — exhaustive boundary census complete

Fresh auditor `st_01a01e61` classified all 26 ancestry commits, 134 touched paths, 20 Todo boundaries, and both shared-writer boundaries. It confirms the complete remaining unaccepted set has exactly two rows:

1. `F4-B3-K-CLOSE-RIDE`: Todo 4 tooling/captures landed in `c265f2d`, with core-file corrections in `9218be9` and `dfb899b`, rather than riding close commit `8e6a86b`.
2. `F4-B4-TODO8-Y`: Todo 8's complete five-REFUSE evidence landed inside mixed Wave-1 checkpoint `065a18f`, despite its `Commit: Y` single-lane requirement.

All other boundaries are compliant or already covered by exact B1/B2 owner acceptance. Current bytes and product scope remain clean. One final bounded owner disposition can close both without rewriting history; no additive commit can change their first placement.

## Transition — remaining historical deviations accepted

Owner statement (2026-08-20): `Approve remaining historical deviations`.

Accepted, and only these additional census-proven rows:

1. `F4-B3-K-CLOSE-RIDE`: Todo 4 tooling/captures landed in `c265f2d`, with core corrections in `9218be9` and `dfb899b`, instead of close commit `8e6a86b`.
2. `F4-B4-TODO8-Y`: Todo 8's complete five-REFUSE evidence landed inside mixed Wave-1 checkpoint `065a18f` instead of its own `Commit: Y` lane.

No history rewrite and no other exception are authorized. Because F1's prior APPROVE predated this complete census and missed both rows, F1 is reopened alongside F4 until both original reviewers verify the completed disposition record.

## Transition — complete four-row disposition committed

Writer `st_01a01e55` committed `5e66140978f50258025a557180a8f0c3bc63b1de` with the exact six-path completed record: both prior review artifacts, exhaustive census, extended owner disposition, operational plan, and primary Plan-37 record.

The record preserves both owner statements, all four and only four accepted deviations, the 26-commit/134-path completeness proof, original requirements, no rewrite, no other exception, EXECUTING status, and reopened F1/F4 checkboxes.

## Transition — F4 APPROVE after exhaustive re-audit

Original F4 reviewer `st_01a01e4a` approved commit `5e66140` with zero open findings after independently reproducing the complete 26-commit/134-path census. B1-B4 remain recorded as unmet historical requirements and are ACCEPTED/CLOSED under the two exact owner statements. No fifth defect, rewrite, broad exception, or product-scope change exists.

F4 is checked. F2/F3 remain approved. F1 final census-backed re-audit remains active and unchecked; Todo 20 remains blocked.

## Transition — F1 APPROVE after exhaustive census re-audit

Original F1 reviewer `st_01a01e47` approved commit `5e66140` with zero open findings. The exhaustive 26-commit/134-path census is independently reproduced; B1-B4 are ACCEPTED/CLOSED under the two exact owner statements; no fifth deviation exists; product/compliance matrix remains green; status stays EXECUTING.

F1 is checked. F1-F4 all APPROVE. Todo 20 remains blocked pending the plan-required explicit owner okay on the surfaced final-wave results.

## Transition — final-wave owner okay

Owner statement (2026-08-20): `okay`.

This is the explicit okay on the surfaced F1-F4 APPROVE results. Todo 20 is now unblocked: consecutive `npm run check` ×2, then status flips for plans/37, plans/36, and AGENTS.md in one pass, discovery-pin confirmation, Todo-20 evidence, and the authorized EXECUTED commit.

## Transition — Todo 20 blocked on fixture drift

Executor `st_01a01e79` stopped after first `npm run check` exit 1. No status flip, no second check, no commit.

Cause: `test/markdown-reifier.test.ts` requires `test/fixtures/extract/self-hosting-carrier/specs/carrier/markdown-authoring.sdp.md.txt` byte-identical to live carrier. Live is `readiness: ready` (Todo 16 / `7f768d1`); fixture is still `defined`. Only that one READY Spec has a reifier fixture.

Also noted: preflight will fail on current nonignored untracked `.omo` parity/ultrawork/task-20 paths unless they are staged or committed before the gate. Dirty tracked files alone do not fail preflight.

Next: fix the fixture, then retry Todo 20 with untracked Plan-37/.omo checkpoint state made preflight-clean.

## Transition — workspace checkpoint before Todo 20

Preflight fails on nonignored untracked `.omo` files that belong to the paused parity plan, the ultrawork notepad, and the ledger. These are recoverable workspace state, not Plan-37 product. A separate workspace-state checkpoint will be committed so `npm run check` can pass cleanly.
