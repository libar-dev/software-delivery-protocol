
> @libar-dev/software-delivery-protocol@0.0.0 test
> node ./vitest-test.mjs test/cli-import.test.ts


 RUN  v2.1.9 /Users/darkomijic/dev-projects/libar-software-delivery-protocol

 ❯ test/cli-import.test.ts (7 tests | 7 failed) 28ms
   × sdp import > documents the import verb 7ms
     → expected 'sdp — Libar Software Delivery Protoco…' to contain 'sdp import <path...> [--dry-run]'
   × sdp import > prints would-be Markdown without writing when dry-run is requested 4ms
     → expected 1 to be +0 // Object.is equality
   × sdp import > writes an imported document beside its TypeScript source 4ms
     → expected 1 to be +0 // Object.is equality
   × sdp import > refuses an existing Markdown target without overwriting it 5ms
     → expected 'sdp — Libar Software Delivery Protoco…' to contain 'import/target-exists'
   × sdp import > refuses Pack carriers and continues with other files 3ms
     → expected 'sdp — Libar Software Delivery Protoco…' to contain 'import/pack-unsupported'
   × sdp import > rejects an unknown import flag by name 2ms
     → expected 'sdp — Libar Software Delivery Protoco…' to be 'sdp import: unknown option --exclude\n' // Object.is equality
   × sdp import > renders refusal findings and exits 1 3ms
     → expected 'sdp — Libar Software Delivery Protoco…' to contain '[info] import/refusal'

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 7 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  test/cli-import.test.ts > sdp import > documents the import verb
AssertionError: expected 'sdp — Libar Software Delivery Protoco…' to contain 'sdp import <path...> [--dry-run]'

- Expected
+ Received

- sdp import <path...> [--dry-run]
+ sdp — Libar Software Delivery Protocol
+ Usage:
+   sdp --help
+   sdp build [root] [--exclude PATH]... [--check-clean]
+   sdp validate [root] [--exclude PATH]... [--check-clean]
+   sdp view [root] [--exclude PATH]... [--check-clean]
+
+ Commands:
+   build      Extract every *.sdp.ts and *.sdp.md under root (default: cwd), plus the anchor
+               constants in the other *.ts/*.tsx source files, into <root>/generated/graph.json — then derive the
+              executable contracts (per-example step contracts + per-parent space contracts,
+              the A2 mechanism) into <root>/generated/contracts/. Exits 1 and writes nothing on
+              any hard error — the emitted artifacts are all-or-nothing. --check-clean
+              additionally runs a second independent extraction + generation and fails on any
+              byte divergence (the determinism self-check). Repeat --exclude PATH to omit exact
+              root-relative POSIX path prefixes from both extraction surfaces.
+   validate   build, then run the conformance + honesty checks over the one graph (one
+              validation path). A check error exits 1; gaps and orphans inform as warnings.
+              graph.json is still written when the checks fail — the graph is the faithful
+              projection; check errors describe the repo's conformance, not the artifact.
+   view       validate, then generate the Design Review — the one read-only human view, a pure
+              projection of the graph — into <root>/generated/design-review/ (rewritten
+              wholesale, so no stale page survives). The view is written even when checks
+              fail: findings render in it, which is what a review surface is for. Exit code
+              follows validate. --check-clean additionally re-renders independently and fails
+              on any byte divergence.

 ❯ test/cli-import.test.ts:39:27
     37|     // When: consumers inspect the command list.
     38|     // Then: the durable import invocation is discoverable.
     39|     expect(SDP_HELP_TEXT).toContain("sdp import <path...> [--dry-run]"…
       |                           ^
     40|   });
     41| 

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/7]⎯

 FAIL  test/cli-import.test.ts > sdp import > prints would-be Markdown without writing when dry-run is requested
AssertionError: expected 1 to be +0 // Object.is equality

- Expected
+ Received

- 0
+ 1

 ❯ test/cli-import.test.ts:58:24
     56| 
     57|       // Then: it emits the document to stdout and leaves no sibling b…
     58|       expect(exitCode).toBe(0);
       |                        ^
     59|       expect(capture.readStdout()).toBe(`=== ${join(root, "specs", "or…
     60|       expect(existsSync(join(root, "specs", "order.sdp.md"))).toBe(fal…

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/7]⎯

 FAIL  test/cli-import.test.ts > sdp import > writes an imported document beside its TypeScript source
AssertionError: expected 1 to be +0 // Object.is equality

- Expected
+ Received

- 0
+ 1

 ❯ test/cli-import.test.ts:80:24
     78| 
     79|       // Then: the Markdown sibling lands and the TypeScript source re…
     80|       expect(exitCode).toBe(0);
       |                        ^
     81|       expect(readFileSync(join(root, "nested", "order.sdp.md"), "utf8"…
     82|       expect(existsSync(sourcePath)).toBe(true);

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/7]⎯

 FAIL  test/cli-import.test.ts > sdp import > refuses an existing Markdown target without overwriting it
AssertionError: expected 'sdp — Libar Software Delivery Protoco…' to contain 'import/target-exists'

- Expected
+ Received

- import/target-exists
+ sdp — Libar Software Delivery Protocol
+ Usage:
+   sdp --help
+   sdp build [root] [--exclude PATH]... [--check-clean]
+   sdp validate [root] [--exclude PATH]... [--check-clean]
+   sdp view [root] [--exclude PATH]... [--check-clean]
+
+ Commands:
+   build      Extract every *.sdp.ts and *.sdp.md under root (default: cwd), plus the anchor
+               constants in the other *.ts/*.tsx source files, into <root>/generated/graph.json — then derive the
+              executable contracts (per-example step contracts + per-parent space contracts,
+              the A2 mechanism) into <root>/generated/contracts/. Exits 1 and writes nothing on
+              any hard error — the emitted artifacts are all-or-nothing. --check-clean
+              additionally runs a second independent extraction + generation and fails on any
+              byte divergence (the determinism self-check). Repeat --exclude PATH to omit exact
+              root-relative POSIX path prefixes from both extraction surfaces.
+   validate   build, then run the conformance + honesty checks over the one graph (one
+              validation path). A check error exits 1; gaps and orphans inform as warnings.
+              graph.json is still written when the checks fail — the graph is the faithful
+              projection; check errors describe the repo's conformance, not the artifact.
+   view       validate, then generate the Design Review — the one read-only human view, a pure
+              projection of the graph — into <root>/generated/design-review/ (rewritten
+              wholesale, so no stale page survives). The view is written even when checks
+              fail: findings render in it, which is what a review surface is for. Exit code
+              follows validate. --check-clean additionally re-renders independently and fails
+              on any byte divergence.
+
+ Unknown command: import
+

 ❯ test/cli-import.test.ts:106:36
    104|       // Then: it reports the refusal and preserves the existing conte…
    105|       expect(exitCode).toBe(1);
    106|       expect(capture.readStderr()).toContain("import/target-exists");
       |                                    ^
    107|       expect(readFileSync(targetPath, "utf8")).toBe("# Existing\n");
    108|     } finally {

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/7]⎯

 FAIL  test/cli-import.test.ts > sdp import > refuses Pack carriers and continues with other files
AssertionError: expected 'sdp — Libar Software Delivery Protoco…' to contain 'import/pack-unsupported'

- Expected
+ Received

- import/pack-unsupported
+ sdp — Libar Software Delivery Protocol
+ Usage:
+   sdp --help
+   sdp build [root] [--exclude PATH]... [--check-clean]
+   sdp validate [root] [--exclude PATH]... [--check-clean]
+   sdp view [root] [--exclude PATH]... [--check-clean]
+
+ Commands:
+   build      Extract every *.sdp.ts and *.sdp.md under root (default: cwd), plus the anchor
+               constants in the other *.ts/*.tsx source files, into <root>/generated/graph.json — then derive the
+              executable contracts (per-example step contracts + per-parent space contracts,
+              the A2 mechanism) into <root>/generated/contracts/. Exits 1 and writes nothing on
+              any hard error — the emitted artifacts are all-or-nothing. --check-clean
+              additionally runs a second independent extraction + generation and fails on any
+              byte divergence (the determinism self-check). Repeat --exclude PATH to omit exact
+              root-relative POSIX path prefixes from both extraction surfaces.
+   validate   build, then run the conformance + honesty checks over the one graph (one
+              validation path). A check error exits 1; gaps and orphans inform as warnings.
+              graph.json is still written when the checks fail — the graph is the faithful
+              projection; check errors describe the repo's conformance, not the artifact.
+   view       validate, then generate the Design Review — the one read-only human view, a pure
+              projection of the graph — into <root>/generated/design-review/ (rewritten
+              wholesale, so no stale page survives). The view is written even when checks
+              fail: findings render in it, which is what a review surface is for. Exit code
+              follows validate. --check-clean additionally re-renders independently and fails
+              on any byte divergence.
+
+ Unknown command: import
+

 ❯ test/cli-import.test.ts:147:36
    145|       // Then: the Pack is named, while the valid sibling still writes.
    146|       expect(exitCode).toBe(1);
    147|       expect(capture.readStderr()).toContain("import/pack-unsupported"…
       |                                    ^
    148|       expect(existsSync(join(root, "order.sdp.md"))).toBe(true);
    149|     } finally {

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[5/7]⎯

 FAIL  test/cli-import.test.ts > sdp import > rejects an unknown import flag by name
AssertionError: expected 'sdp — Libar Software Delivery Protoco…' to be 'sdp import: unknown option --exclude\n' // Object.is equality

- Expected
+ Received

- sdp import: unknown option --exclude
+ sdp — Libar Software Delivery Protocol
+ Usage:
+   sdp --help
+   sdp build [root] [--exclude PATH]... [--check-clean]
+   sdp validate [root] [--exclude PATH]... [--check-clean]
+   sdp view [root] [--exclude PATH]... [--check-clean]
+
+ Commands:
+   build      Extract every *.sdp.ts and *.sdp.md under root (default: cwd), plus the anchor
+               constants in the other *.ts/*.tsx source files, into <root>/generated/graph.json — then derive the
+              executable contracts (per-example step contracts + per-parent space contracts,
+              the A2 mechanism) into <root>/generated/contracts/. Exits 1 and writes nothing on
+              any hard error — the emitted artifacts are all-or-nothing. --check-clean
+              additionally runs a second independent extraction + generation and fails on any
+              byte divergence (the determinism self-check). Repeat --exclude PATH to omit exact
+              root-relative POSIX path prefixes from both extraction surfaces.
+   validate   build, then run the conformance + honesty checks over the one graph (one
+              validation path). A check error exits 1; gaps and orphans inform as warnings.
+              graph.json is still written when the checks fail — the graph is the faithful
+              projection; check errors describe the repo's conformance, not the artifact.
+   view       validate, then generate the Design Review — the one read-only human view, a pure
+              projection of the graph — into <root>/generated/design-review/ (rewritten
+              wholesale, so no stale page survives). The view is written even when checks
+              fail: findings render in it, which is what a review surface is for. Exit code
+              follows validate. --check-clean additionally re-renders independently and fails
+              on any byte divergence.
+
+ Unknown command: import


 ❯ test/cli-import.test.ts:163:34
    161|     // Then: invocation fails before any import work begins.
    162|     expect(exitCode).toBe(1);
    163|     expect(capture.readStderr()).toBe("sdp import: unknown option --ex…
       |                                  ^
    164|   });
    165| 

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[6/7]⎯

 FAIL  test/cli-import.test.ts > sdp import > renders refusal findings and exits 1
AssertionError: expected 'sdp — Libar Software Delivery Protoco…' to contain '[info] import/refusal'

- Expected
+ Received

- [info] import/refusal
+ sdp — Libar Software Delivery Protocol
+ Usage:
+   sdp --help
+   sdp build [root] [--exclude PATH]... [--check-clean]
+   sdp validate [root] [--exclude PATH]... [--check-clean]
+   sdp view [root] [--exclude PATH]... [--check-clean]
+
+ Commands:
+   build      Extract every *.sdp.ts and *.sdp.md under root (default: cwd), plus the anchor
+               constants in the other *.ts/*.tsx source files, into <root>/generated/graph.json — then derive the
+              executable contracts (per-example step contracts + per-parent space contracts,
+              the A2 mechanism) into <root>/generated/contracts/. Exits 1 and writes nothing on
+              any hard error — the emitted artifacts are all-or-nothing. --check-clean
+              additionally runs a second independent extraction + generation and fails on any
+              byte divergence (the determinism self-check). Repeat --exclude PATH to omit exact
+              root-relative POSIX path prefixes from both extraction surfaces.
+   validate   build, then run the conformance + honesty checks over the one graph (one
+              validation path). A check error exits 1; gaps and orphans inform as warnings.
+              graph.json is still written when the checks fail — the graph is the faithful
+              projection; check errors describe the repo's conformance, not the artifact.
+   view       validate, then generate the Design Review — the one read-only human view, a pure
+              projection of the graph — into <root>/generated/design-review/ (rewritten
+              wholesale, so no stale page survives). The view is written even when checks
+              fail: findings render in it, which is what a review surface is for. Exit code
+              follows validate. --check-clean additionally re-renders independently and fails
+              on any byte divergence.
+
+ Unknown command: import
+

 ❯ test/cli-import.test.ts:195:36
    193|       // Then: the shared finding renderer names the refusal and the c…
    194|       expect(exitCode).toBe(1);
    195|       expect(capture.readStderr()).toContain("[info] import/refusal");
       |                                    ^
    196|     } finally {
    197|       rmSync(root, { recursive: true, force: true });

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[7/7]⎯

 Test Files  1 failed (1)
      Tests  7 failed (7)
   Start at  17:33:02
   Duration  1.02s (transform 234ms, setup 0ms, collect 587ms, tests 28ms, environment 0ms, prepare 84ms)

