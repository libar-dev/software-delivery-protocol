# Todo 36 decorative-anchor rejection probe

Command:

```sh
rg -n 'spec:consumers\.edit-model' src --glob '*.ts'
```

Result: no matches, exit status 1.

The source tree contains no single entrypoint that realizes the edit-model behavior. In contrast, the
reader's concrete surfaces are present:

```sh
rg -n 'createReader|BlastRadius|blastRadius' src/reader/reader.ts
```

```text
217:export interface BlastRadius {
261:  blastRadius(changedFiles: readonly string[]): BlastRadius;
366:export function createReader(graph: GraphSchema): Reader {
707:  const blastRadius = (changedFiles: readonly string[]): BlastRadius => {
828:    blastRadius,
```

An `edit-model` code anchor would bind a speculative location rather than a realizing entrypoint. The
spec therefore remains defined, unanchored, and unverified.
