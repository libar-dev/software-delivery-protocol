# Task 18 Markdown reifier totality evidence

Command:

```text
PATH="$PWD/node_modules/.bin:$PATH" node ./vitest-test.mjs test/markdown-grammar-hardening.test.ts -t "item h"
```

The focused probe replaces the body parser with an unexpected throw, the path that escaped the
public Markdown reifier before this task. The public call now returns rather than throws:

```text
validatorId: extract/invalid-frontmatter
line: 1
message: the carrier could not be reified: unexpected body failure

Test Files  1 passed (1)
Tests       1 passed | 13 skipped (14)
```
