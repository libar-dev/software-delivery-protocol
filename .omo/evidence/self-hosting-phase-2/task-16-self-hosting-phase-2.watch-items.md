# Task 16 watch-item and ruling record

## Section 6

All five watch items are unfired after the checkout migration:

- Table-sugar syntax: multi-case behavior is expressed as sibling `example` Specs, not tables.
- Single-literal vocabulary form: emitted vocabulary was idiomatic and no concrete slot needed it.
- Multi-entry constraint form: the migrated constraint uses the ruled one-entry form.
- Array-section prose sub-owner: no migrated section produced ambiguous prose ownership.
- Markdown Pack syntax: no caller forced Markdown authoring; the Pack remains a TS manifest.

The sole session-2 ruling under fire is the Design Review Option 1 decision. It applies one
escaping policy to all rendered fields and lexicographically sorts dynamic keys. It is a reversible
projection representation fix, so it is not admitted as a durable decision Spec.

## Section 8

`done s2` now records the migration/delta catalog entry, bound-example count verification,
fixture-to-live byte identity, the Design Review cluster, and the five unfired watch-item entries.
The existing dynamic-key ordering and escaping rows remain `done s2` and match Option 1.
