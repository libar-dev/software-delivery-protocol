# Session 3 Close Evidence: G3 Hardening Baseline

## Proposed disposition

G3 is the Session 3 hardening-baseline gate. It is pending owner disposition. The proposed
acceptance SHA is `0bb200a` (`fix(check-temporal): exclude .omo workspace plans from temporal-token scan`).
This temporal-fix blocker was required because the tracked `.omo` workspace plan contains
temporal-by-genre plan references that must not enter the temporal-token scan.

No gate stamp, acceptance date, or accepted SHA has been recorded in either gate ledger.

## Evidence

- Grammar-hardening cluster: `fcd5cef` (`fix(extract): land the grammar-hardening cluster (review-06)`),
  with `test/markdown-grammar-hardening.test.ts` green (14 tests).
- Executed refusal-parity matrix: `f19694d` (`test(extract): execute the bounded TS↔Markdown
  refusal-parity matrix`), with `test/extract-parity.test.ts` green (10 tests) and probe fixtures
  under `test/fixtures/import/parity/`.
- Parser-spec claim: `f71616d` (`feat(specs): resolve the parser hardening non-claim with pinned
  parity evidence`), recorded in `specs/carrier/markdown-parser.sdp.md`.
- Design Review: `generated/design-review`, regenerated by `npm run check`.

## Gate verification

After the ledger correction at the proposed acceptance state:

- `npm run check` exited 0 (426 tests passed; the regenerated self-hosting Design Review contains 18 pages).
- `node ./check-self-hosting-gates.mjs` exited 0.

The owner should review this evidence and record the disposition before Atlas stamps G3 or begins
the canonical-default flip.
