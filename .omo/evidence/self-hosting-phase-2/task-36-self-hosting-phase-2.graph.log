# Todo 36 graph QA

Command:

```sh
npm run generate:self-hosting
```

Output:

```text
52 specs · 1 packs · 31 anchors → 84 nodes · 162 edges (0 errors, 0 warnings)
Wrote generated/graph.json
Wrote generated/contracts (4 modules)
validate: 0 errors · 0 warnings (conformance + honesty over the one graph)
Wrote generated/design-review (54 pages)
```

Graph probe:

```sh
rg -n -C 2 'spec:consumers\.(reader|edit-model)|impl:protocol\.reader' generated/graph.json
```

The graph contains `spec:consumers.reader` and `spec:consumers.edit-model`. The reader has declared
`refines` to `spec:consumers.agent-surface` and two anchored `satisfies` bindings:
`impl:protocol.reader` and `impl:protocol.reader-impact`. The edit-model has declared `refines` to
`spec:consumers.projections-model` and its Pack membership only.
