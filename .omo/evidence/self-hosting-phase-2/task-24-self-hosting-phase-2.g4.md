# G4 Owner Gate Evidence: Canonical-Default Flip

## Proposed disposition

G4 is the HARD phase-internal owner gate for the canonical-default flip. The proposed
acceptance SHA is `0a06882` (`feat(carrier): the canonical-default flip — Markdown by default, TS as import source and per-ID option`).

No gate stamp, acceptance date, or accepted SHA has been recorded in the gate ledger yet.

## Evidence

### Flip commit

- `0a06882` — `feat(carrier): the canonical-default flip — Markdown by default, TS as import source and per-ID option`
- Operative records updated: `docs/concept/DECISIONS.md`, `CONTEXT.md`, `AGENTS.md`, `docs/concept/04-authoring-and-binding.md`, `docs/concept/README.md`, `docs/concept/00-vision-scope-and-mvp-boundary.md`, `docs/concept/07-mvp-roadmap-and-open-questions.md`, `jtbd-stories/`, root `README.md`, package docs.
- `check-carrier-interim.mjs` renamed to `check-carrier-rule.mjs`; all stale references removed.
- `check-carrier-truth.mjs` re-pinned: interim/Gate-4 strings moved to OBSOLETE, post-flip pins in CLAIMS, TS-mention RULES audit retained.

### Clean-clone proof

- The original clean-clone proof remains recorded below; the amended flip at `0a06882` passed the full repository `npm run check` gate.
- Full log: `.omo/evidence/self-hosting-phase-2/task-23-self-hosting-phase-2.clean-clone.log`.

### Acceptance criteria verification

1. **Flip is total and recorded** — `find specs examples/checkout-v1/specs -name "*.sdp.ts" ! -name "*.pack.sdp.ts"` returns empty. The two lawful Pack manifests are `specs/self-hosting.pack.sdp.ts` and `examples/checkout-v1/specs/checkout.pack.sdp.ts`. Dual-carrier fixtures remain intact (`test/fixtures/extract/duplicate-id/`). Negative proof recorded in `.omo/evidence/self-hosting-phase-2/task-23-self-hosting-phase-2.fixture.log`.
2. **Round-trip fidelity and installed-package proof** — `test/import-round-trip.test.ts` passes in the clean clone; `test/package-smoke.test.ts` passes; packed tarball consumer verifies `sdp import --dry-run` and the barrel. Details: `.omo/evidence/self-hosting-phase-2/task-23-self-hosting-phase-2.acceptance.md`.
3. **Hardening claim landed** — `specs/carrier/markdown-parser.sdp.md` carries the bounded parity claim with six same-class findings and four named non-claims, landed at `f71616d`.
8. **`sdp import` documented** — `CONTEXT.md` glossary row includes the key phrase `many source adapters`; CLI help text and checkout/README mention the verb; `node_modules/.bin/sdp import --dry-run` works from the packed tarball. Details: `.omo/evidence/self-hosting-phase-2/task-23-self-hosting-phase-2.acceptance.md`.
f. **Anti-misleading sweep** — `04` §1 now states that per-ID canonical-surface config is designed-for and deferred (**ASPIRATIONAL**); the active realization is file-existence-only, where the existing surface is canonical and the other is generated read-only. `check-carrier-rule.mjs` and `check-carrier-truth.mjs` both pass post-flip.

### Audit scripts

- `node ./check-carrier-rule.mjs` exits 0.
- `node ./check-carrier-truth.mjs` exits 0.
- `node ./check-self-hosting-gates.mjs` exits 0.

### Design Review

- `generated/design-review` regenerated by the clean-clone `npm run check`.

## Owner disposition

The owner should review this evidence and the linked todo-23 acceptance file before Atlas stamps G4
or begins any corpus-wave work (wave 6+).
