AGENTS.md
CLAUDE.md
CONTEXT.md
docs/**
jtbd-stories/**
plans/**
.sisyphus/**
.tmp-scratch/**

# Archived review artifacts - verbatim historical records, not format-policed
reviews/

# Exploration exhibits — verbatim register sketches (surface micro-implementations); their
# exact formatting IS the content, never format-policed (the gen-1 playground discipline)
explorations/

# The golden graph oracle carries the serializer's exact bytes (serialize.ts owns all output
# bytes); reformatting it would break the byte-compare against extractor output
test/fixtures/checkout-v1/expected-graph.json

# The golden Design Review carries the renderer's exact bytes (design-review.ts owns them);
# same rule for the generated artifacts themselves — derived output is never format-policed
test/fixtures/checkout-v1/expected-design-review/
generated/

# The ruled carrier grammars own these bytes; Prettier rewrites can break their grammar
# and fixture↔live byte-parity
*.sdp.md
*.sdp.gherkin

# Refusal-parity carriers are intentionally malformed inputs read as text by the reifiers.
test/fixtures/import/parity/
