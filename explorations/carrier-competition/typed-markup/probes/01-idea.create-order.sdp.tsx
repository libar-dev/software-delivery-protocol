// Illustrative evidence only: the markup export does not exist. CLOSED.md records why
// building it merely to typecheck this probe would distort the timeboxed comparison.

import { Spec } from "@libar-dev/software-delivery-protocol/markup";
export default (
  <Spec
    id="spec:orders.create-order"
    kind="behavior"
    altitude="feature"
    readiness="idea"
    refines="spec:orders.order-management"
    title="Customer creates an order"
  />
);
